# Trakt Dashboard 优化升级计划（2026-06-08）

来源：优化审计 Workflow `w9qzsjr7n`。
目标：按 P0 → P3 分阶段完成稳定性、正确性、开发质量、架构与运维改进。
跟踪约定：`[ ]` 未完成，`[x]` 已完成。

## 执行原则

- [x] 每个 Task 完成后补充/更新测试（测试 47 → 80）。
- [x] 每个阶段完成后运行 `pnpm lint`、`pnpm typecheck`、`pnpm test`（每阶段收尾门禁通过）。
- [~] 涉及提交时按提交工作流执行：版本已对齐到 0.41.0；实际 commit/push 待用户确认（当前在 main，按 harness 规则未自动提交）。
- [x] 涉及外部 API/队列/数据库语义变更时，先补回归测试再改实现。

---

## Phase 0 — P0：关键稳定性与质量门禁

### [x] P0-T01：让每用户同步执行互斥且队列安全

优先级：P0
目标：避免同一用户多个 full/incremental sync 并发执行、重复入队、状态互相覆盖。

- [x] 将 `syncState.status` 的 read-then-write 改成原子条件更新：仅非 `running` 时可切换为 `running`。
- [x] 为手动 `enqueueSyncNow` 增加稳定 per-user dedup `jobId`。
- [x] `/sync/trigger` 对已运行任务返回明确 `202`/`409` 与当前状态，不再静默重复触发。
- [x] 增加并发触发回归测试：双请求只启动一个同步任务。
- [ ] 影响文件：`apps/api/src/services/sync.ts`、`apps/api/src/routes/sync.ts`、`apps/api/src/jobs/scheduler.ts`。

### [x] P0-T02：强化 OAuth token 并发刷新

优先级：P0
目标：避免 Redis lock 等待超时后返回过期 token，减少 Trakt 401/刷新雪崩。

- [x] lock 等待循环结束后重新读取 DB token 并检查是否仍有 >= 5 分钟有效期。
- [x] 若仍过期，失败时抛出包含 status/body 的 typed error；DB update 竞争丢失时重新读取 fresher token。
- [x] lock 轮询改为 bounded backoff + jitter。
- [x] 增加并行过期、慢 lock holder、刷新失败路径测试。
- [ ] 影响文件：`apps/api/src/services/trakt.ts`。

### [x] P0-T03：新增 CI 工作流并接入 lint/typecheck/test

优先级：P0
目标：PR 与 main push 自动质量门禁。

- [x] 新增 `.github/workflows/ci.yml`，触发 `push: main` 与 `pull_request`。
- [x] CI 步骤：checkout → setup pnpm/node → `pnpm install --frozen-lockfile` → `pnpm lint` → `pnpm typecheck` → `pnpm test`。
- [x] 配置 Turbo/GitHub cache，减少 CI 时间。
- [x] 确认 CI 对失败测试/类型错误返回非零退出码。
- [ ] 影响文件：`.github/workflows/ci.yml`、`package.json`、`turbo.json`。

### [x] P0-T04：修正 Docker publish workflow 的默认分支触发

优先级：P0
目标：默认分支 main 可正常构建/发布镜像。

- [x] 将 `.github/workflows/docker-publish.yml` 的 `branches: [github]` 改为 `branches: [main]`。
- [x] 将 `publish-release` 条件同步改为 `refs/heads/main`。
- [x] 校验 `VITE_API_BASE` GitHub repo var/secret 是否存在：workflow 保留 repo var fallback 空值，未做远端 secrets 查询。
- [x] 手动检查 tag `v*` 触发逻辑仍正常。
- [ ] 影响文件：`.github/workflows/docker-publish.yml`。

### [x] P0-T05：抽取重复工具函数 `parseBoundedInt` 与 `withTimeout`

优先级：P0
目标：统一参数边界解析与超时行为，避免多处漂移。

- [x] 新建 `apps/api/src/lib/utils.ts` 或等价公共模块。
- [x] 统一导出 `parseBoundedInt`。
- [x] 统一导出 `withTimeout`，后续 Phase 1 可再升级为 AbortController 版本。
- [x] 替换 routes/services 中重复实现。
- [x] 增加工具函数单测。
- [ ] 影响文件：`apps/api/src/routes/shows.ts`、`apps/api/src/routes/movies.ts`、`apps/api/src/services/trakt.ts`、`apps/api/src/services/sync.ts`、`apps/api/src/services/tmdb.ts`、`apps/api/src/lib/validate.ts`。

### [x] P0-T06：移除 Drizzle 类型缺失导致的 `(as any)` cast

优先级：P0
目标：让 `originalLanguage`、`translatedOverview`、`translatedTitle` 等字段具备真实类型保护。

- [x] 运行/修正 `pnpm db:generate`，确认 schema 与 inferred select 类型一致。
- [x] 检查 `packages/db/src/schema.ts` 与导出类型是否覆盖新增字段。
- [x] 移除 `apps/api/src/routes/shows.ts` 中相关 `(as any)`。
- [x] 如 Drizzle 版本/类型推导有兼容问题，升级或显式修复 `$inferSelect` 使用方式。
- [x] 增加类型回归检查，确保字段重命名会在编译期失败。
- [ ] 影响文件：`apps/api/src/routes/shows.ts`、`packages/db/src/schema.ts`、`packages/db/src/index.ts`。

---

## Phase 1 — P1：同步可靠性、API 契约、测试基线与构建体验

### [x] P1-T01：增量同步纳入电影历史与 Watchlist 新鲜度

优先级：P1
目标：scheduled incremental sync 不再只更新剧集历史。

- [x] 为 movie history 增加增量 cursor。
- [x] 为 watchlist 增加轻量 reconcile 或 `watchlist_synced_at`。
- [x] 将 movie/watchlist 更新接入 incremental sync 路径。
- [x] 增加仅增量同步场景测试：电影历史与 watchlist 可刷新。
- [ ] 影响文件：`apps/api/src/services/sync.ts`、`apps/api/src/jobs/scheduler.ts`。

### [x] P1-T02：升级 provider HTTP retry/timeout 行为

优先级：P1
目标：Trakt/TMDB 请求具备统一 retry、timeout、AbortController、Retry-After 解析。

- [x] 建立共享 HTTP client helper（`apps/api/src/lib/http.ts`）。
- [x] 支持网络错误、5xx、429 的指数退避 + jitter。
- [x] 支持 `Retry-After` 秒数与 HTTP-date 两种格式。
- [ ] 使用 `AbortController` 真正取消超时请求（仍用 Promise.race，下阶段升级）。
- [x] 输出结构化错误元数据（TraktApiError）。
- [ ] 影响文件：`apps/api/src/services/trakt.ts`、`apps/api/src/services/tmdb.ts`。

### [x] P1-T03：让 Watchlist mutation 最终一致

优先级：P1
目标：避免 Trakt 成功但本地失败、本地删除受远端临时错误阻塞。

- [x] DB 写失败后 fire-and-forget `syncWatchlist` 后台 reconcile。
- [x] 抛出原始错误，前端/API 响应仍反映失败状态。
- [x] 增加 DB failure after Trakt success 测试。
- [ ] 影响文件：`apps/api/src/routes/watchlist.ts`、`apps/api/src/services/sync.ts`。

### [x] P1-T04：控制翻译抓取 fanout 与主动限流

优先级：P1
目标：减少 full sync 中 TMDB 多语言 season fetch 的调用爆炸。

- [x] force-refresh 仅针对最近 14 天内播出（或 air date 未知）的缺失标题，旧剧集不再每次全语言重抓。
- [x] 增加 per-provider token bucket/rate limiter（`apps/api/src/lib/rate-limit.ts`，接入 `providerFetch`、TMDB、Trakt）。
- [x] 缓存按 language 分键的 season translation set，读取时按 fallback chain 做 locale resolution。
- [x] 增加回归测试：token bucket 限流行为 + force-refresh 14 天窗口门控（`rate-limit.test.ts`、`sync-force-refresh.test.ts`）。
- [x] 影响文件：`apps/api/src/services/sync.ts`、`apps/api/src/services/tmdb.ts`、`apps/api/src/services/trakt.ts`、`apps/api/src/lib/http.ts`、`apps/api/src/lib/rate-limit.ts`。

### [x] P1-T05：新增 Vitest coverage 脚本与最低覆盖率阈值

优先级：P1
目标：测试覆盖率可见、可在 CI 阻断明显退化。

- [x] 为 web/API 添加 `test:coverage` 脚本。
- [x] API 补充 `@vitest/coverage-v8` 依赖。
- [x] Turbo 增加 `test:coverage` task（outputs: coverage/\*\*）。
- [x] 设置初始阈值为「略低于当前覆盖率」的 floor（API ~28%、web ~13% lines），后续逐步上调；CI 退化即失败。
- [x] CI 接入 coverage：根 `ci` 脚本由 `pnpm test` 改为 `pnpm test:coverage`。
- [x] 影响文件：`apps/web/package.json`、`apps/api/package.json`、`turbo.json`、`package.json`、Vitest config。

### [x] P1-T06：显式添加 web/API Vitest 配置

优先级：P1
目标：测试环境、setupFiles、coverage 配置不再隐式依赖约定。

- [x] 新建 `apps/web/vitest.config.ts`，配置 `environment: 'jsdom'` 与 `setupFiles`，并从 `vite.config.ts` 移除内联 test 块。
- [x] 新建 `apps/api/vitest.config.ts`，配置 `environment: 'node'`。
- [x] 配置 include/exclude 与 coverage reporter（text/json-summary/html）。
- [x] 确认现有测试全部通过（API 41、web 16）。
- [x] 影响文件：`apps/web/vitest.config.ts`、`apps/api/vitest.config.ts`、`apps/web/vite.config.ts`。

### [x] P1-T07：统一 API Dockerfile 运行时并升级 Bun

优先级：P1
目标：避免 Node 安装依赖 + Bun 运行造成 native addon/运行时差异。

- [x] 运行时镜像由 `oven/bun:1.1-alpine` 升级到 `oven/bun:1.2-alpine`（与 workspace 目标 bun-types 对齐）。
- [x] 确认并文档化 pnpm install + Bun runtime 兼容策略：运行时依赖全部为纯 JS / Bun 兼容，无 native addon ABI 漂移，故继续用 pnpm 解析（与 lockfile/CI 一致）。
- [x] 移除重复 `COPY packages/db/drizzle`（`COPY packages/ ./packages/` 已包含）。
- [~] 构建镜像并运行 API smoke test —— 当前环境无 Docker daemon，无法本地构建；改由 CI/`scripts/build-images.sh` 验证。
- [x] 影响文件：`apps/api/Dockerfile`。

### [x] P1-T08：引入 Prettier/格式化门禁

优先级：P1
目标：统一格式，减少大范围 UI/样式修改中的风格漂移。

- [x] 安装 `prettier` 与 `eslint-config-prettier`。
- [x] 新增 `prettier.config.mjs`（4-space/double-quote/trailing-comma，贴合既有主流风格）+ `.prettierignore`。
- [x] 根 `package.json` 增加 `format` 与 `format:check`；`lint` 接入 `prettier --check .`。
- [x] CI lint 阶段接入 `prettier --check .`（经 `pnpm lint`）。
- [x] `eslint.config.mjs` 末尾接入 `eslint-config-prettier` 关闭风格冲突规则。
- [x] 全仓 `prettier --write .` 在 Phase 1 收尾统一执行。
- [x] 影响文件：`package.json`、`eslint.config.mjs`、`prettier.config.mjs`、`.prettierignore`。

### [x] P1-T09：将关键 ESLint warn 升级为 error

优先级：P1
目标：a11y、hooks、any-type 违规能阻断 CI。

- [x] 修复现有告警：19 个 `no-unused-vars`（含删除死代码 `upsertShowFromTmdb`）、15 个 `no-explicit-any`、2 个 `prefer-const`、6 个 `jsx-a11y`。
- [x] 升级为 error：`no-unused-vars`、`no-explicit-any`、`prefer-const`、三个 `jsx-a11y` 规则；测试文件豁免 `no-explicit-any`。
- [x] 按策略保留 7 个非阻断 warning：`react-hooks/set-state-in-effect`（6）+ `exhaustive-deps`（1），均为「从异步数据同步本地 state」的可接受模式。
- [x] `eslint .` 现为 0 error / 7 warning，exit 0。
- [x] 影响文件：`eslint.config.mjs` 及 ~15 个源码文件。

### [x] P1-T10：标准化 API response envelope

优先级：P1
目标：统一 `{ ok, data }`、分页与错误响应，减少前端分支判断。

- [x] `ApiResponse<T>` 增加 `ok?: true`；新增 canonical discriminated union `ApiResult<T>` 与 `ApiErrorResponse`。
- [x] `PaginatedResponse<T>` 已存在并补 `ok?: true`。
- [x] 新增 Hono helper：`apiOk` / `apiError` / `apiPaginated`（`apps/api/src/lib/response.ts`）。
- [x] 迁移 auth（error 响应）、calendar（成功响应）到 helper；shows/movies/sync/watchlist 已发 `{ ok, data }` 兼容形态，可增量改用 helper。
- [x] `apps/web/src/lib/api.ts` 已统一使用 `ApiResponse<T>` / `PaginatedResponse<T>`（兼容，无破坏）。
- [x] 影响文件：`packages/types/src/index.ts`、`apps/api/src/lib/response.ts`、`apps/api/src/routes/auth.ts`、`apps/api/src/routes/calendar.ts`。

### [x] P1-T11：让 packages/types/db 具备构建产物与版本策略

优先级：P1
目标：workspace 包不再只暴露 raw TypeScript，版本与根项目策略一致。

- [x] 为 `packages/types` 添加 `tsconfig.build.json`，`pnpm build` 输出 `dist` + `.d.ts` + declarationMap。
- [x] `packages/types` 增加 `exports` 映射与 `files`；main/types 保持指向 `src`（保留 Bun 直跑 TS / Docker 拷源 的运行模型，避免破坏 runtime；私有包不发布故无需切 dist）。
- [x] `packages/db` 明确为 workspace-internal source-only（drizzle 运行时），增加 `exports` 映射。
- [x] 统一版本策略：固定 root version，全部包对齐到 `0.40.1`（与提交工作流记忆一致）。
- [x] Turbo `build.outputs` 已为 `dist/**`，types 构建产物纳入缓存；`dist` 已 gitignore。
- [x] 影响文件：`packages/types/package.json`、`packages/types/tsconfig.build.json`、`packages/db/package.json`、`apps/api/package.json`、`apps/web/package.json`。

### [x] P1-T12：引入 Hono RPC / API contract 层（基础层）

优先级：P1
目标：消除手写 `apps/web/src/lib/api.ts` 与 API routes 的路径/类型重复维护。

- [x] 从 `apps/api/src/index.ts` 导出 `AppType`（`typeof routes`）。
- [x] api `package.json` 增加 type-only 子路径导出 `./app-type`（仅 `types`，无 runtime 入口，避免误执行服务端代码）。
- [x] web 端新增 `apps/web/src/lib/rpc.ts`：`hc<AppType>` typed client（并行保留旧 `api` client）。
- [~] 逐路由迁移 hooks —— 完整 RPC 推断还需把各 route 文件改成 method-chaining，属较大且有风险的增量重构；当前以「基础层 + 并行客户端」落地，hooks 迁移留作增量（计划本身即定义为逐路由迁移）。web typecheck 验证未引入服务端类型图耦合问题。
- [x] 影响文件：`apps/api/src/index.ts`、`apps/api/package.json`、`apps/web/src/lib/rpc.ts`、`apps/web/package.json`。

### [x] P1-T13：Calendar query 参数边界与 watched 状态

优先级：P1
目标：避免超大日期范围扫描，并让日历可区分已观看/未观看。

- [x] 使用 P0 抽取的 `parseBoundedInt` 解析 `before`/`after`。
- [x] 设置最大值 90 天（`MAX_WINDOW_DAYS`）。
- [x] `CalendarEpisode` 增加 `watched: boolean`。
- [x] API query 用 correlated `EXISTS(watch_history)` 计算 watched（避免 LEFT JOIN 行扇出）。
- [x] 前端 Calendar UI 在剧集卡上展示「已观看」徽标。
- [~] 路由级回归测试随 P2-T10 `calendar.test.ts` 一并补充。
- [x] 影响文件：`apps/api/src/routes/calendar.ts`、`packages/types/src/index.ts`、`apps/web/src/pages/CalendarPage.tsx`。

---

## Phase 2 — P2：正确性修复、运维安全与架构硬化

### [x] P2-T01：修复 reset cursor 对 next episode 与 lastWatchedAt 的正确性

优先级：P2

- [x] 抽取 `getResetAt` + `postResetWatchWhere` + 导出 `getPostResetWatchedEpisodeIds(userId, showId)`。
- [x] `watchedIds`、`nextEpisodeId` 排除逻辑、`lastWatchedAt`（含 `NULLS LAST`）、watched count 全部统一应用同一 reset cursor。
- [x] `computeWatchedEpisodes` 改为复用 `getPostResetWatchedEpisodeIds`（消除 count 与 ids 漂移）。
- [~] reset 后 count/nextEpisode/lastWatchedAt 回归测试随 P2-T10 `sync.test.ts` 落地。
- [x] 影响文件：`apps/api/src/services/sync.ts`。

### [x] P2-T02：避免 nullable timestamp 序列化导致 500

优先级：P2

- [x] 新增 DTO helper：`toIsoOrNull(d)`（`apps/api/src/lib/datetime.ts`，null/invalid → null，不抛错）。
- [x] 将 routes 中 11 处 `X?.toISOString() ?? null` 统一替换为 `toIsoOrNull(X)`（shows/movies/sync）。
- [x] 核对 schema：`createdAt`/`lastSyncedAt`/`addedAt`/`listedAt` 已为 `notNull`（约束已满足），`watch_history.watchedAt`/`lastWatchedAt` 等可空项均已 guard。
- [x] 增加 `toIsoOrNull` 回归测试（null/Date/string/invalid）。
- [x] 影响文件：`apps/api/src/lib/datetime.ts`、`apps/api/src/routes/shows.ts`、`apps/api/src/routes/movies.ts`、`apps/api/src/routes/sync.ts`。

### [x] P2-T03：移除 episode 创建中的静默 `userId=0` fallback

优先级：P2

- [x] 将 `findOrCreateEpisode` 的 `userId` 改为必填（移到 `language?` 之前，并移除未使用的 `language` 形参）。
- [x] 更新两处 call site 显式传入上下文 `userId`；缺失即编译期失败（比 runtime throw 更强）。
- [x] 移除 `userId ?? 0` 静默兜底。
- [~] 缺失 episode 创建路径测试随 P2-T10 `sync.test.ts` 落地。
- [x] 影响文件：`apps/api/src/services/sync.ts`。

### [x] P2-T04：改进 metadata cache 策略与 stale-while-revalidate

优先级：P2

- [x] 新增 `cachedProviderFetch` 通用包装：fresh 命中短路；provider 失败时在 grace window（TTL+14d）内返回 stale cache，不抛错。
- [x] 按 source 设置 TTL：show/movie/season 30d、episode 7d，默认 7d（`CACHE_TTL_HOURS` map）。
- [x] Trakt episode ratings 加 24h cache + stale fallback，不再每次详情 live fetch。
- [x] cleanup job 删除阈值由 7d 改为 `METADATA_MAX_AGE_HOURS`（30d+14d），避免误删仍有效的 30d metadata。
- [x] 影响文件：`apps/api/src/services/tmdb.ts`、`apps/api/src/services/trakt.ts`、`apps/api/src/jobs/scheduler.ts`。

### [x] P2-T05：增加结构化 sync observability 与生产诊断

优先级：P2

- [x] 新增 `apps/api/src/lib/observability.ts`：每次 sync 生成 run ID（correlation ID）。
- [x] 记录 provider HTTP count（tmdb/trakt 分项）、retry count、429 frequency、error count、duration（hook 进 `providerFetch`）。
- [x] 诊断数据保存在进程内 last-run 指标（单用户自托管无需 DB 迁移），full/incremental sync 完成时输出结构化日志。
- [x] `/sync/health` 端点返回 `{ queueAvailable, currentRun, lastRun }`，生产安全。
- [x] 增加 observability 单测（counters、no-run 安全、finalize duration）。
- [x] 影响文件：`apps/api/src/lib/observability.ts`、`apps/api/src/lib/http.ts`、`apps/api/src/services/sync.ts`、`apps/api/src/routes/sync.ts`。

### [x] P2-T06：统一 workspace 包版本策略

优先级：P2

- [x] 采用 fixed root version 策略（与提交工作流记忆一致），不引入 Changesets。
- [x] 修正 mismatch：root / api / web / db / types 全部对齐到 `0.41.0`。
- [x] 版本更新流程文档化于提交工作流记忆与 README「Version Cadence」；本批次通过 sed 一次性对齐。
- [x] 影响文件：`package.json`、`packages/db/package.json`、`packages/types/package.json`、`apps/api/package.json`、`apps/web/package.json`。

### [x] P2-T07：优化 Turbo test/db task 依赖图

优先级：P2

- [x] `test` / `test:coverage` 由 `^build` 改为 `^typecheck`（types/db 已具 typecheck script）。
- [x] 为 `db:migrate` 增加 `dependsOn: ["db:generate"]`。
- [x] 确认改动后 `pnpm test` 6/6 通过。
- [x] 影响文件：`turbo.json`。

### [x] P2-T08：为 nginx 增加基础安全 headers

优先级：P2

- [x] 添加 `X-Frame-Options SAMEORIGIN`（always）。
- [x] 添加 `X-Content-Type-Options nosniff`（always）。
- [x] 添加 `Referrer-Policy strict-origin-when-cross-origin`（always）。
- [x] CSP 以 `Content-Security-Policy-Report-Only` 模式评估。
- [x] API/auth proxy 增加 `X-Forwarded-Proto $scheme`。
- [x] 影响文件：`apps/web/nginx.conf`。

### [x] P2-T09：移除/条件化内部 Nexus registry 配置

优先级：P2

- [x] `bunfig.toml` 默认改为 `https://registry.npmjs.org/`，注释说明可用 `BUN_CONFIG_REGISTRY` 本地覆盖（保留供 Docker COPY）。
- [x] `scripts/build-images.sh` 默认 `NPM_REGISTRY` / `APK_MIRROR` 改为公共上游，env 可覆盖。
- [x] Dockerfile 已支持 `ARG NPM_REGISTRY` 外部覆盖（默认公共）。
- [x] CI 使用 pnpm + 公共 registry，无需私有 registry override。
- [x] 影响文件：`bunfig.toml`、`scripts/build-images.sh`。

### [x] P2-T10：扩展 API 测试覆盖

优先级：P2

- [x] 覆盖 calendar route：空结果、按 airDate 分组 + watched 标志、越界 before/after 不崩溃。
- [x] 覆盖 sync route：idle 触发、running 重复触发 409、队列不可用 503、`/sync/health` 两态。
- [x] 为 sync service 增加 reset cursor 边界测试（`getPostResetWatchedEpisodeIds`：post-reset 去重/丢 null、无 cursor 返回全部）+ rate limiter 单测（P1-T04）。
- [x] 复用 `p2-api-flows.test.ts` mock 基建（扩展 `selectDistinct`），避免重复 ~200 行 mock；测试总数 31 → 55。
- [x] 影响文件：`apps/api/src/__tests__/p2-api-flows.test.ts`。

### [x] P2-T11：Settings route 迁移到 Zod/validateBody

优先级：P2

- [x] 定义 `updateSettingsSchema`（displayLanguage / syncIntervalMinutes 1..10080 / httpProxy http(s)|""|null）。
- [x] 用 `validateBody(c, schema)` 替换 inline regex/integer validation。
- [x] 统一错误 `details` 结构（复用 `validateBody` 的 `{ error, details }`）。
- [x] 增加测试：非法 sync interval(0)→400、非 http(s) proxy→400、空串 proxy→200 清空。
- [x] 影响文件：`apps/api/src/routes/settings.ts`、`apps/api/src/lib/validate.ts`、`apps/api/src/__tests__/p2-api-flows.test.ts`。

### [x] P2-T12：抽取语言 fallback chain 到共享 i18n 包

优先级：P2

- [x] 新建 `packages/i18n` workspace 包（source-only，与 types/db 同策略）。
- [x] 导出 `LANGUAGE_FAMILY_VARIANTS`、`getLanguageFamily`、`buildLanguageFallbackChain`。
- [x] API sync 改为从 `@trakt-dashboard/i18n` 引用（删除本地副本）；web i18n 目前不重复此逻辑（UI 文案 fallback 与内容语言 fallback 是不同关注点），共享包可供其按需采用。
- [x] 增加 fallback chain 单测（6 例：family 提取、locale 优先、原语言变体、未知语言合成、去重、null 安全）。
- [x] 影响文件：`packages/i18n/*`、`apps/api/src/services/sync.ts`、`apps/api/package.json`。

### [x] P2-T13：建立 TypeScript project references / `tsc -b`

优先级：P2

- [x] leaf packages 增加 `composite: true` build config（types/db/i18n `tsconfig.build.json`，emit `.d.ts` + tsbuildinfo）。
- [x] apps tsconfig 增加 `references` 指向 packages build config（api→types/db/i18n，web→types）。
- [x] root `tsconfig.build.json` 汇总 references；新增 `pnpm typecheck:build`（`tsc -b`）。
- [x] 保留 turbo per-package `typecheck` 为主 CI gate（已验证不受 references 影响），`tsc -b` 作为增量加速的附加路径——避免 bundler resolution / web noEmit 与 composite 的冲突。
- [x] `.tsbuildinfo` 已 gitignore；`tsc -b` 与 `pnpm typecheck` 均通过。
- [x] 影响文件：`apps/web/tsconfig.json`、`apps/api/tsconfig.json`、`packages/*/tsconfig.build.json`、`tsconfig.build.json`、`package.json`、`.gitignore`。

### [x] P2-T14：Redis/BullMQ 队列健康检查与 fallback

优先级：P2

- [x] 新增 `isQueueHealthy()`（Redis `ping` + 2s 超时，永不抛错）。
- [x] `/sync/trigger` 在 enqueue 失败时返回 503 `{ ok:false, error:"queue_unavailable" }`，不再假成功。
- [x] 新增 `GET /sync/health` 端点返回 `{ queueAvailable }`（healthy 200 / down 503）。
- [x] 增加测试：enqueue 失败→503、/sync/health 健康/不可用两态。
- [~] 单用户 in-process fallback 为可选项，未实现（保持 503 显式失败语义更安全）。
- [x] 影响文件：`apps/api/src/jobs/scheduler.ts`、`apps/api/src/routes/sync.ts`、`apps/api/src/__tests__/p2-api-flows.test.ts`。

---

## Phase 3 — P3：性能细化、流程规范与长期维护

### [x] P3-T01：优化 next-episode 查询，避免大数组 `NOT IN`

优先级：P3

- [x] next-episode 查询由 `id NOT IN (watchedIds 数组)` 改为 cursor-aware `NOT EXISTS` 相关子查询 anti-join。
- [x] 新增复合索引 `watch_history(user_id, episode_id)`（migration `0008`）；`episodes(show_id, …)` 已有 show_id + 唯一 (show,season,episode) 索引覆盖排序。
- [~] 大规模性能测试用真实查询计划验证（需真实 DB，单测不易模拟）；逻辑正确性由 reset-cursor 测试与编译期类型保证。
- [x] 影响文件：`apps/api/src/services/sync.ts`、`packages/db/src/schema.ts`、`packages/db/drizzle/0008_*.sql`。

### [x] P3-T02：建立 `.plan/` 与需求/计划追踪规范

优先级：P3

- [x] 创建 `.plan/` 目录并纳入 git。
- [x] 新增 `.plan/requirement.md` 记录当前功能集、非功能需求与约束。
- [x] `.plan/README.md` 明确计划文件命名/状态规范（根目录现有 `plan-20260608.md` 历史保留，新计划入 `.plan/`）。
- [~] CI 上传 `.plan/` artifact 为可选项，未启用。
- [x] 影响文件：`.plan/README.md`、`.plan/requirement.md`。

### [x] P3-T03：加入 commitlint / conventional commits enforcement

优先级：P3

- [x] 安装 `@commitlint/cli` 与 `@commitlint/config-conventional`（+ husky、lint-staged）。
- [x] 新增 `commitlint.config.mjs`（extends config-conventional，纯校验无副作用）。
- [x] 使用 Husky 添加 `commit-msg` hook（`commitlint --edit`），`prepare: husky` 激活 core.hooksPath。
- [x] 添加 `lint-staged`（`.lintstagedrc.json`：staged 文件 `prettier --write`）+ `pre-commit` hook。
- [x] 验证：合法 conventional 消息通过、非法消息被拒。
- [x] 影响文件：`package.json`、`commitlint.config.mjs`、`.lintstagedrc.json`、`.husky/commit-msg`、`.husky/pre-commit`。
- 注：commit-msg 为只读校验，与记忆中被移除的「after-commit 版本号/cherry-pick」副作用 hook 性质不同，不会引发冲突循环。

### [x] P3-T04：为 web docker-compose 服务增加健康检查/依赖条件

优先级：P3

- [x] 为 `web` 增加 nginx healthcheck（busybox `wget` 探测 `http://localhost/`）。
- [x] 增加 `depends_on: api: condition: service_started`（nginx 运行时反代，无需等待 api healthy）。
- [~] `docker compose up --wait` 语义需真实 docker 验证；当前环境无 daemon，已校验 compose 结构。
- [x] 影响文件：`docker-compose.yml`。

### [x] P3-T05：集中管理 React Query query keys

优先级：P3

- [x] 新建 `apps/web/src/lib/queryKeys.ts`（typed factory，group `.all` 前缀 + 具体 key 函数）。
- [x] 覆盖 auth/sync-status/sync-debug/stats/settings/now-playing/shows-progress/show-detail/show-history/calendar/episode-detail/episode-history/movies-progress/movie-detail/movie-history/watchlist。
- [x] 替换 `apps/web/src/hooks/index.ts` 中全部裸字符串数组（query + invalidate + remove，~40 处）。
- [x] invalidateQueries 统一走同一 factory；web typecheck + 16 测试通过。
- [x] 影响文件：`apps/web/src/lib/queryKeys.ts`、`apps/web/src/hooks/index.ts`。

### [x] P3-T06：拆分/强化 app-specific ESLint 与格式配置

优先级：P3

- [x] 分层 flat config：共享 TS 规则（no-explicit-any/no-unused-vars/prefer-const）+ web 块 + api/packages 块。
- [x] web 块（`apps/web/**`）：browser globals + React-Hooks + jsx-a11y 规则。
- [x] api/packages 块（`apps/api/**`、`packages/**`）：node globals，不再误挂 React/a11y 规则。
- [~] lint 仍走根 `eslint .`（已快且正确）；未改为 turbo per-package（包 lint 脚本当前是 typecheck 别名，拆分价值低、风险高）。
- [x] `eslint .` 仍 0 error / 7 warning，exit 0。
- [x] 影响文件：`eslint.config.mjs`。

---

## 阶段验收清单

### [x] Phase 0 验收

- [x] 同一用户重复触发 sync 只产生一个运行实例。
- [x] 并发 token refresh 不会返回过期 token。
- [x] `main` push/PR 具备 CI 质量门禁。
- [x] Docker publish workflow 使用正确默认分支。
- [x] 重复工具函数已统一，相关 `any` cast 已消除。

### [x] Phase 1 验收

- [x] incremental sync 覆盖 episodes/movies/watchlist。
- [x] Trakt/TMDB HTTP 行为统一支持 retry/timeout（cancel 仍 Promise.race，已记录）。
- [x] Watchlist mutation 具备最终一致策略。
- [x] coverage、Vitest config、Prettier、ESLint gate 已接入 CI。
- [x] API response contract 与 web client 类型更一致（envelope helpers + 分页类型）。

### [x] Phase 2 验收

- [x] reset cursor、nullable timestamp、userId 必填等正确性问题有回归测试。
- [x] metadata cache 支持 stale fallback，清理 TTL 分 source。
- [x] sync observability、Redis health、nginx security headers 已上线。
- [x] TS project references 与 Turbo task graph 更稳定。

### [x] Phase 3 验收

- [x] next episode 查询对大历史记录更高效（NOT EXISTS + 复合索引）。
- [x] 计划、提交、compose health、queryKeys、lint config 等长期维护项完成。
- [x] 后续改进可通过 checklist 持续追踪。
