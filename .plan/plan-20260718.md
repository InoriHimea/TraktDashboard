# Plan 2026-07-18 — Trakt 观看历史重复记录审计与清理（N2-T17）

## 背景

`jellyfin-plugin-trakt-ex`（外部仓库）曾把重复的"已观看"记录写入用户的 Trakt.tv
账号：根因 1（绝对集数编号剧集每天被误判"未记录"，已在插件端修复，历史脏数据还
在）、根因 2（同一集短间隔重复出现，未查清）。本计划为 trakt-dashboard 新增一个
"审计 + 人工确认 + 清理"能力：从本地数据库找出 Trakt 历史里的重复记录，按记录间
隔区分"疑似 bug 重复"与"真实重看"，用户确认后调用 Trakt 官方接口从 **Trakt.tv
本身**永久删除（不能只删本地镜像），同时暴露间隔分布数据供后续排查根因 2。

关键发现（详见批次 1 的实现记录）：本地 `watch_history` 表已经是 Trakt 历史的逐
条镜像（`trakt_play_id` 唯一约束对应 Trakt 历史条目自己的数字 id），审计可以完全
跑本地 SQL，不需要重新调用 Trakt API；但不能按 `source='trakt'` 过滤——剧集同步
路径（`triggerIncrementalSync`）从未显式设置该字段，落到 schema 默认值
`'manual'`，只有电影路径（`syncMovies`）会写 `'trakt'`，按 `source` 过滤会让根因
1 影响的剧集类目完全漏检；正确谓词是 `trakt_play_id IS NOT NULL`。现有单条历史
删除路由（`shows.ts`/`movies.ts`）只删本地行、从不调用 Trakt，是一个潜在 bug（手
动删除的记录会被下次同步复活），本计划顺带修复。

## 执行原则

- 后端先行、前端依赖后端接口稳定后再接，两个批次可跨会话延续。
- 涉及"从 Trakt.tv 永久删除数据"这种不可逆操作，测试全部走 mock，绝不在测试里
  调用真实 Trakt API；真实删除调用建议用户在少量数据上手动验证一次响应体契约。
- 每个批次：对应 workspace 的 tsc/typecheck 全绿、全套件测试全绿（只增不减）、
  独立 commit。
- 不新增数据库迁移——复用现有 `watch_history` 表和现有索引。

## 批次清单

### 批次 1 — 后端：Trakt 删除能力 + 审计查询 + 两个新路由 + 顺带修复旧删除路由 `[x]`

- [x] `services/trakt.ts` 新增 `removeFromHistory(userId, ids)`，`POST
  /sync/history/remove`，body `{ids}`，返回 `TraktRemoveHistoryResult`。
- [x] 新增 `services/history-duplicates.ts`：分组查询（`trakt_play_id IS NOT
  NULL`，`HAVING count(*) > 1`，join episodes/shows/movies）+ 纯函数
      `clusterBursts(entries, windowHours)`（链式聚类，覆盖"整组跨度过大导致真
      实重看误漏检"这个坑）。
- [x] `routes/history.ts` 新增 `GET /history/duplicates?windowHours=72`、`POST
  /history/duplicates/remove`（分批调用 Trakt、Set 去重后 recalc 受影响的
      show/movie 进度）。
- [x] 顺带修复 `shows.ts`/`movies.ts` 的单条历史删除路由，删本地行前先调用
      `removeFromHistory`（`trakt_play_id` 为空则跳过，只删本地）。
- [x] `packages/types/src/index.ts` 新增 `HistoryDuplicateEntry`/
      `HistoryDuplicateGroup`/`RemoveHistoryDuplicatesResult`。
- [x] 测试：`removeFromHistory`（trakt-client 表驱动）、两个新路由（mock db +
      mock trakt client + mock sync 服务）、`clusterBursts` 纯函数边界用例、
      `shows.ts`/`movies.ts` 修复后的新增 Trakt 调用分支。
- [x] **踩坑**：api 包的测试命令是 `vitest --run`（不是 `bun test`——`bun test`
      运行时没有 `vi.hoisted`，会直接报错），本文件"验收标准"一节已有描述写
      错，实际验证一律用 `npx vitest run` / `pnpm test:coverage`。
- [x] **踩坑**：`db.select().from(watchHistory).innerJoin(episodes, ...)` 这种
      不带显式列选择的 join 查询，返回行的按表名嵌套规则没有把握时不要猜——
      直接改成显式 `.select({id: ..., traktPlayId: ...})` 拉扁平字段，绕开
      歧义，顺便也减少了过度查询。
- [x] **踩坑**：单条删除（`shows.ts`/`movies.ts`，用户主动点一次）和批量删除
      （`POST /duplicates/remove`，审计页面一次性删多条）对"Trakt 调用失败怎么
      办"要用不同的容错策略——单条删除失败就应该整体报错（否则又会退化成
      "只删本地"的老 bug）；批量删除按 100 个一批处理，某一批失败时只跳过那一
      批（不删对应本地行，留给下次重试），不阻塞其余批次，这是刻意的不对称
      设计，不是疏漏。
- 验收：新建 3 个测试文件（`history-duplicates.test.ts` 12 个、
  `history-duplicates-routes.test.ts` 10 个、`trakt-client.test.ts` +1 个）+
  扩充 `shows-routes.test.ts`（+3）、`movies-routes.test.ts`（+2）。api 侧
  795→**823** 个测试全绿，覆盖率 85.8%/72.9%/87.3%/87.6%→**86.2%/73.2%/87.7%/
  87.9%**（stmts/branch/funcs/lines），阈值同步小幅上调至 86/73/87/87（本批次
  是功能开发而非专项覆盖率清零，未刻意追加额外测试去拉高数字，涨幅是新代码
  自带测试的自然结果）。

### 批次 2 — 前端：审计页面 + 入口 + 测试 `[ ]`

- [ ] `lib/api.ts` 的 `history` 域下加 `duplicates.list`/`duplicates.remove`。
- [ ] `lib/queryKeys.ts` 新增 `historyDuplicates`（独立顶层 key，不复用
      `history`/`history-infinite`）。
- [ ] `hooks/index.ts` 新增 `useHistoryDuplicates`/`useRemoveHistoryDuplicates`。
- [ ] 新页面 `pages/HistoryDuplicatesPage.tsx` + 路由 `/history/duplicates`：分
      组卡片、间隔展示、burst 默认勾选/长间隔不勾选、`ConfirmDialog` 二次确认、
      顶部统计（含"短间隔 burst 数"供排查根因 2）。
- [ ] `HistoryPage.tsx` Header 按钮组加入口链接。
- [ ] `locales/zh-CN.json` + `locales/en-US.json` 同步新增 `historyDuplicates.*`
      （`parity.test.ts` 兜底）。
- [ ] 测试：新页面 render/interaction 测试。

## 验收标准

- 后端：`cd apps/api && npx vitest run` + `npx tsc --noEmit` 全绿；前端：
  `cd apps/web && npx vitest run` + `npx tsc --noEmit` 全绿。
- 手动用浏览器预览验证：本地插入模拟重复数据 → 审计页面分组/聚类/勾选状态符合
  预期 → 删除流程走 mock 或小范围真实数据验证响应体契约。
- 完成后更新 `.plan/README.md` 状态行、`.plan/requirement.md` 新增 N2-T17 行。
