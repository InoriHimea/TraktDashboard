# Plan 2026-07-15b — N3-T02 覆盖率回升第六批：web 侧启动

## 背景

api 侧四个大文件（sync.ts/trakt.ts/shows.ts/history.ts）已在
plan-20260715.md 十个批次内全部清零，api 整体 69.58%→85.82%
（stmts/branch/funcs/lines：85.8%/72.9%/87.3%/87.6%）。N3-T02 剩余范围
是 web 侧（`apps/web`），本轮启动该部分。

web 侧当前实测覆盖率（`vitest run --coverage`）：**25.45%/21.9%/
18.24%/26.26%**（stmts/branch/funcs/lines），阈值 25/17/24/20，与
`vitest.config.ts` 注释记录的 2026-07-12 基线一致（web 侧自那之后未
再更新）。

与 api 侧不同，web 侧的覆盖缺口不是集中在少数几个巨型文件里，而是
**分散在 35+ 个组件/页面文件**，总计约 15700 行原始代码（对应约 2773
条 statement）。已有 17 个测试文件、160 个测试，覆盖了：
`App.tsx`（54%）、`i18n.ts`（98.7%）、`queryKeys.ts`（已 100%，参见
plan-20260712.md）、`components/ui/*`（94%+）、部分 `hooks/index.ts`
（13/76 个 hook：useNote/useUpsertNote/useDeleteNote/useLists/
useCreateList/useAddListItem/useRatings/useSetRating/useRemoveRating/
useWatchlist/useAddToWatchlist/useRemoveFromWatchlist/
useMarkSeasonWatched）、以及 `HistoryPage`/`SettingsPage`/
`ShowDetailPage`/`StatsPage`/`EpisodeDetailPage` 五个页面（部分覆盖，
40-70% 不等）。

零覆盖或极低覆盖的主要文件（按体量分层，行数为原始文件行数）：

**基础设施层（api.ts/hooks/push.ts）**——高杠杆，模式已验证：

- `src/lib/api.ts`（586 行，11.3%）——所有后端请求的唯一入口，`request<T>()`
  共享 helper + 按域分组的方法（auth/shows/movies/history/jellyfin/
  search/discover/watchlist/ratings/notes/lists/collection/backup/
  settings...），结构与 api 侧 `services/trakt.ts` 的"共享 fetch helper +
  一堆薄封装方法"如出一辙，可用同样的表驱动 + mock `fetch` 策略测试。
- `src/hooks/index.ts`（896 行，17.1%，共 76 个导出 hook，13 个已测）——
  React Query hook 集合，绝大多数是 `useQuery`/`useMutation` 包一层
  `api.xxx.yyy()`；已验证的测试模式（`renderHook` + mock `../../lib/api`）
  可直接套用到剩余 63 个。
- `src/lib/push.ts`（137 行，1.8%）——Web Push 订阅/注销/VAPID 密钥比对，
  api 侧 `lib/push.ts`（发送端，已 100%）的浏览器端对应物，需要 mock
  `Notification`/`navigator.serviceWorker`/`PushManager` 等浏览器 API。

**小型展示型组件/页面（各 <260 行，多为纯渲染 + 少量交互）**：
`Layout.tsx`（70）、`ShowCard.tsx`（184）、`SeasonTab.tsx`（104）、
`ProgressBar.tsx`（81）、`TraktProgressBar.tsx`（32）、
`DateTimePickerModal.tsx`（140）、`SlidingPanel.tsx`（169）、
`EpisodeSeasonStrip.tsx`（204）、`MediaListPage.tsx`（224，
`src/components/` 下的共享组件，非 `pages/`）、`MoviesPage.tsx`（62）、
`TVShowsPage.tsx`（63）、`WatchlistPage.tsx`（240）。

**stats + JellyfinStatsPage 小组件**（`src/pages/stats/*`、
`src/pages/JellyfinStatsPage/*`，多为图表/卡片，各 100-430 行）：
`MediaComposition`（113）、`RatingDistribution`（115）、
`ActivityChart`（164）、`WatchPatterns`（145）、`WatchHeatmap`（206）、
`RecentActivity`（288）、`TraktStats`（248）、`ScreenTime`（429）；
`ActivityFeed`（137）、`LibraryOverview`（134）、`PlayHeatmap`（175）、
`TopContent`（181）、`index.tsx`（136）。

**中大型组件/页面（260-900 行）**：`EpisodeGrid`（196）、
`UpNextBanner`（254）、`WatchHistoryPanel`（368）、`SearchModal`（389）、
`TopNav`（393）、`DiscoverPage`（403）、`CalendarPage`（447）、
`NowPlayingPopup`（540）、`EpisodeInfoCard`（554）、`LoginPage`（567）、
`MovieDetailPage`（664）、`JellyfinTab.tsx`（740）、`HeroSection`（889）。

**超大型页面（>850 行，体量堪比 api 侧的 sync.ts 子集）**：
`ListsPage.tsx`（857）、`CollectionPage.tsx`（1179）、
`settings/BackupTab.tsx`（1488，全仓库 web 侧最大单文件）。

## 执行原则

- 与 api 侧不同，本计划**不追求一次性覆盖全部文件**——体量分散在 35+
  个文件，逐一"读全文→写测试→验证→提交"的节奏预计需要跨越多个会话；
  本文件按层级/主题分组批次，后续任何一次执行都可以从某个批次继续，
  不需要重新做结构分析。
- 复用已验证的测试模式：
    - 页面组件：`render` + `QueryClientProvider`（`retry:false`）+
      `MemoryRouter`，mock `../../hooks` 与/或 `../../lib/api`（参照
      `HistoryPage.test.tsx`/`ShowDetailPage.test.tsx`）。
    - hooks：`renderHook` + `QueryClientProvider` wrapper，mock
      `../../lib/api`（参照 `notes-lists-ratings.test.tsx`）。
    - `lib/api.ts` 本身：`vi.stubGlobal("fetch", ...)`，不 mock
      `../../lib/api`——直接验证 URL/method/body 构造与错误处理（参照
      api 侧 `trakt-client.test.ts` 的表驱动写法）。
- 纯展示型组件（无数据请求、无副作用）优先用最简单的 `render` +
  `screen` 断言，不需要 QueryClientProvider/mock。
- 每个子批次：`tsc --noEmit`（web 包）+ 全套件测试全绿（只增不减）+
  `vitest run --coverage` 确认涨幅 + 独立 commit；不阈值造假——ratchet
  设在实测值以下。
- 大型/超大型文件（HeroSection/JellyfinTab/MovieDetailPage/
  ListsPage/CollectionPage/BackupTab）执行前必须先通读全文，具体测试
  分支在通读后于本文件对应批次补充细化（沿用 api 侧
  plan-20260715.md 对 sync.ts 的方法论）。

## 批次清单

### 批次 1 — `src/lib/api.ts`（586 行）✅

- [x] `request<T>()` 共享 helper：GET 不设 `Content-Type`，非 GET
      设置；`credentials: "include"` 始终携带；默认 base 为 `/api`，
      `auth.*` 用空字符串 base；非 2xx 响应解析 `{error}` 消息并挂载
      `status` 到抛出的 Error 上，JSON 解析失败时降级用 `res.statusText`。
- [x] 表驱动覆盖 GET 方法的 URL/query 拼接（约 50 个）与写操作
      （POST/PUT/DELETE，约 40 个）的 method/body 构造，覆盖全部 19 个
      域命名空间：auth/shows/episodes/sync/stats/settings/trakt/
      calendar/movies/history/watchlist/notifications/ratings/search/
      jellyfin/discover/notes/lists/collection/backup/system。
- [x] `history.export` 特例：不调用 `fetch`，只拼接返回一个 URL 字符串
      （用于 `<a href>` 直接下载），单独验证。
- 验收：新建 `src/lib/__tests__/api.test.ts`（116 个测试）。
  `lib/api.ts` 覆盖率 11.3%→**98.66%** stmts（100% lines / 86.15%
  branch / 100% funcs）。web 整体 25.45%→**30.18%** stmts / 23.61%
  branch / 27.4% funcs / 31.15% lines。

### 批次 2 — `src/hooks/index.ts`：剩余 63 个 hook（896 行，17.1%）✅

- [x] 34 个查询 hook 表驱动验证：api 调用参数正确 + 响应正确解包
      （含多种解包形态：`.then(r=>r.data)`、`.then(r=>r.data??[])`/
      `??null`/`??{movies:[],series:[]}` 默认值兜底、以及
      `useCollectionCheck` 不走 `ApiResponse` 包装直接返回原始响应的
      特例）。
- [x] 23 个变更（mutation）hook 表驱动验证：api 调用参数 + `onSuccess`
      触发的 `invalidateQueries` key 集合正确。**踩坑**：
      `useTriggerSync`/`useTriggerFullSync` 的 `mutationFn` 是裸函数
      引用（`mutationFn: api.sync.trigger`，未包一层箭头函数），
      react-query 会把 `(variables, mutationContext)` 都透传给它——
      断言时只能检查参数的前 N 位，不能用严格的
      `toHaveBeenCalledWith(...expectedArgs)`。
- [x] 单独验证的特殊逻辑 hook：`useNowPlaying`（派生 `isWatching`
      布尔值）、`useInfiniteHistory`（`getNextPageParam` 分页——已加载
      条目数 < total 时返回下一页偏移量，否则返回 `undefined` 终止
      分页）、`useDeleteHistory`（有/无 season+episode 两种不同的
      invalidate 范围）、`useLogout`（`setQueryData`+`removeQueries`，
      不是 `invalidateQueries`）、`useAddToWatchlist`/
      `useDeleteJellyfinSeason`/`useDeleteJellyfinItem`/
      `useMarkEpisodeWatched`。
- 验收：新建 `src/hooks/__tests__/remaining-hooks.test.tsx`（68 个
  测试）。`hooks/index.ts` 覆盖率 17.1%→**99.66%** stmts（99.61%
  lines / 91.52% branch / 99.55% funcs）。web 整体 30.18%→**39.09%**
  stmts / 25.54% branch / 44.54% funcs / 39.65% lines。

### 批次 3 — `src/lib/push.ts`（137 行，1.8%）✅

- [x] `isPushSupported`/`getExistingSubscription`/`fetchVapidPublicKey`：
      支持性检测（`serviceWorker`/`PushManager`/`Notification` 三者
      都存在才算支持）、降级返回 null（fetch 失败/不支持）。
- [x] `enablePush` 全部分支：无 VAPID key → `server-unconfigured`；
      用户拒绝授权 → `permission-denied`；无现有订阅 → 全新 subscribe；
      现有订阅 key 匹配 → 复用（不重新 subscribe）；key 已轮换 →
      unsubscribe 旧订阅后重新 subscribe；unsubscribe 失败但浏览器已
      清理 → 照常走全新 subscribe；unsubscribe 失败但重新检查发现
      **并发标签页已经用新 key 订阅成功** → 直接注册该订阅到后端并提前
      return（不再调用 `pushManager.subscribe`）；unsubscribe 失败且
      重新检查发现旧 key 仍在生效 → 抛 `push-rotation-blocked`；
      unsubscribe 失败且重新检查 `getSubscription()` 本身也失败
      （SW 注册退化）→ 视为已清理，照常走全新 subscribe。
- [x] `disablePush`：无现有订阅 → 直接返回不调用任何 API；有现有订阅 →
      先浏览器 `unsubscribe()` 再通知后端。
- [x] mock 浏览器 API 用 `vi.stubGlobal("Notification"/"PushManager", ...)` + `Object.defineProperty(navigator, "serviceWorker", ...)`；VAPID
      key 用两个可精确字节比对的 base64url 测试串（`"AAAA"`→`[0,0,0]`、
      `"AAAB"`→`[0,0,1]`）避免逐字节手算 base64 解码。
- 验收：新建 `src/lib/__tests__/push.test.ts`（18 个测试）。
  `lib/push.ts` 覆盖率 1.8%→**100%** stmts（100% lines / 90% branch /
  100% funcs）。web 整体 39.09%→**41.03%** stmts / 26.81% branch /
  45.27% funcs / 41.49% lines。**基础设施层（批次 1-3）至此全部完成。**

### 批次 4 — 小型展示型组件（约 954 行）✅

- [x] `Layout.tsx`：mock `./TopNav`（避免连带渲染其 393 行）+
      `../hooks`，验证 username 透传、`settings.displayLanguage` 存在
      时调用 `setLocale`。
- [x] `ShowCard.tsx`：mock `useJellyfinDeleteQueue`/`useDeferJellyfinDelete`
      （`PendingDeleteBadge` 子组件也依赖后者）+ 用真实 `ToastProvider`
      包裹（`useToast` 需要 context）；验证标题/集数/百分比渲染、
      translatedName 优先+原名作副标题、下一集代码渲染。
- [x] `SeasonTab.tsx`：aria-selected 反映 isActive、season 0 显示
      "Specials"、无海报时回退到 "S{n}" 文字。
- [x] `ProgressBar.tsx`/`TraktProgressBar.tsx`：**踩坑**——
      framer-motion 的 `animate` prop 不会在 jsdom 里同步（甚至
      `waitFor` 轮询下也不确定性）反映到 `.style.width`，改为验证
      "不报错渲染"（间接覆盖 ÷0 保护、`Math.min`/`Math.max` clamp 等
      分支的执行）而非断言具体动画样式值。
- [x] `SlidingPanel.tsx`：closed 时不渲染任何内容、open 时渲染
      title/subtitle/children、点击关闭按钮/按 Escape 都调用 onClose。
- [x] `DateTimePickerModal.tsx`：`defaultValue` 正确回填
      `datetime-local` 输入框、确认按钮把输入值转成 ISO 字符串传给
      `onConfirm`、Escape 关闭。
- [x] `EpisodeSeasonStrip.tsx`：mock `react-router-dom` 的
      `useNavigate` + stub `Element.prototype.scrollIntoView`（jsdom
      未实现）；验证空状态、按钮数量与 `aria-current`、点击跳转到正确
      路径、season 0 显示 "Specials"。
- 验收：新建 8 个测试文件（32 个测试）。8 个文件覆盖率
  55.55%-100% stmts（`SlidingPanel.tsx` 最低 55.55%，backdrop/hover
  样式处理分支未覆盖，其余均 83%+）。web 整体 41.03%→**44.24%** stmts
  / 31.04% branch / 47.47% funcs / 44.7% lines。

### 批次 5 — `MoviesPage`/`TVShowsPage`/`WatchlistPage`/`MediaListPage`（约 589 行）✅

- [x] 确认调用关系属实：`MoviesPage.tsx`（62）/`TVShowsPage.tsx`（63）
      都是 `MediaListPage`（224，`src/components/` 下的共享列表组件）的
      薄封装。`MediaListPage` 本身直接测试（filter 按钮/搜索框/清除
      按钮/loading-error-empty-搜索空 四态/`renderItem` 渲染/`headerSlot`/
      键盘快捷键 `/` 聚焦搜索框+focus 态下 `Escape` 清空搜索），达到
      **100%** stmts；`MoviesPage`/`TVShowsPage` 只需 mock 数据 hook +
      卡片子组件验证自身的 prop 透传与 debounce 搜索逻辑。
- [x] `WatchlistPage.tsx`：mock `useWatchlist`/`useRemoveFromWatchlist`；
      验证 show/movie 混合渲染、客户端按标题防抖搜索过滤、
      loading/error/empty 三态、移除成功/失败两种 toast。
- [x] **踩坑（贯穿本批次全部 4 个文件）**：`t()` 未被 mock，会解析出
      真实 zh-CN 翻译文本——断言字面量翻译 key（如
      `"watchlist.empty"`）必然失败；改为断言实际中文文案，或（针对
      `MediaListPage` 自己测试用的 filter fixture）故意使用不存在的
      key 让 `t()` 走"未命中回退返回 key 本身"的逻辑。
- [x] **踩坑**：`toast()` 调用只改变 `ToastProvider` 的 context 状态，
      不会自动出现在 DOM 里——断言 toast 文案出现，必须把 `<Toaster />`
      和 `ToastProvider` 一起渲染，而不是只包 provider。
- 验收：新建 4 个测试文件（25 个测试）。`MediaListPage.tsx` 100%、
  `MoviesPage.tsx` 84.61%、`TVShowsPage.tsx` 92.3%、
  `WatchlistPage.tsx` 90.69%。web 整体 44.24%→**47.16%** stmts /
  33.64% branch / 50.22% funcs / 47.67% lines。

### 批次 6 — stats 小组件（约 1708 行）[x]

- [x] `MediaComposition`（113）、`RatingDistribution`（115）、
      `ActivityChart`（164）、`WatchPatterns`（145）、`WatchHeatmap`
      （206）、`RecentActivity`（288）、`TraktStats`（248）、
      `ScreenTime`（429）。
- [x] **踩坑**：recharts 的 `ResponsiveContainer` 在 jsdom 下测得宽高
      永远是 0，直接不渲染任何图表 DOM（连空 `<svg>` 都没有）——
      `RatingDistribution`/`ActivityChart`/`WatchPatterns` 只能断言
      标题/副标题等静态文案，其 `CustomTooltip` render-prop 函数体
      （仅在真实 hover 时才会被 recharts 调用）也无法覆盖，三个文件
      的覆盖率因此明显低于其余 5 个。不过 `.map()` 生成 `<Cell>` 的
      三元表达式/`barColor()` 调用仍在父组件自身 render 时就已经
      同步执行（JSX 子节点是由父组件 eagerly construct 的，与
      `ResponsiveContainer` 是否真的挂载它们无关），所以传入不同的
      fixture 数据依然对覆盖率有意义。
- [x] **踩坑**：lucide 的 `CalendarDays` 图标本身会渲染一个 SVG
      `<rect>`（图标边框），`WatchHeatmap` 测试里
      `container.querySelector("rect")` 会先命中图标而不是热力图
      单元格——改为先定位热力图自己的 `<svg aria-label="…">` 再在
      其内部查询。
- [x] **踩坑**：jsdom 的内联 style 在读回时会重新序列化颜色值——
      `rgba(139,92,246,1.00)` 这种 alpha=1 的写法会被吐回不带 alpha
      的 `rgb(139, 92, 246)`，且逗号后一律补空格；断言 `.style.fill`
      时改为直接比较 jsdom 序列化后的实际字符串，而不是源码里的字面量。
- 验收：新建 8 个测试文件（32 个测试）。`MediaComposition`/
  `WatchHeatmap`/`RecentActivity`/`TraktStats`/`ScreenTime` 均达
  **100%** stmts；`RatingDistribution` 68.75%、`WatchPatterns`
  76.92%、`ActivityChart` 50%（均受限于上述 recharts 踩坑）。web
  整体 47.16%→**51.92%** stmts / 37.35% branch / 54.07% funcs /
  52.08% lines。

### 批次 7 — `JellyfinStatsPage` 全部（约 763 行）[x]

- [x] `ActivityFeed`（137）、`LibraryOverview`（134）、`PlayHeatmap`
      （175）、`TopContent`（181）、`index.tsx`（136）。
- [x] **踩坑**：`ActivityFeed` 用 `dayjs(...).fromNow()` 显示相对时间，
      需要 `vi.setSystemTime` 固定"现在"才能断言确定的输出；且该组件
      自己没调用 `dayjs.locale("zh-cn")`（只有 `CalendarPage.tsx` 调用），
      dayjs 的 locale 虽是全局单例，但 vitest 默认按文件隔离模块图，
      不会跨测试文件泄漏——因此本文件里 `fromNow()` 渲染的是英文
      （"2 hours ago"），不是中文。
- [x] **踩坑**：lucide 图标的内部注册名可能和导出的组件名不一致——
      `Tv2` 实际渲染的 class 是 `lucide-tv-minimal` 而不是
      `lucide-tv-2`（`tv-2.js` 只是重导出 `tv-minimal.js`）；断言前
      去读了图标源码确认，而不是直接猜测 class 名。
- [x] **踩坑**：jsdom 回读内联 hex 颜色（如 `#38bdf8`）会重新序列化成
      `rgb(56, 189, 248)`，同上批次的 rgba 序列化问题一致，断言前先
      用探测测试跑一遍确认实际值。
- 验收：新建 5 个测试文件（16 个测试），全部达到 **100%** stmts/
  branch/funcs/lines。web 整体 51.92%→**53.94%** stmts / 39.58%
  branch / 55.82% funcs / 54.08% lines。

### 批次 8 — 中型组件 A：`EpisodeGrid`/`UpNextBanner`/`WatchHistoryPanel`/`SearchModal`（约 1207 行）[x]

- [x] `EpisodeGrid`（图片回退链 stillPath→backdrop→占位图+onError 兜底、
      已看对勾、未播出态禁用点击+隐藏更多按钮、更多按钮 stopPropagation）、
      `UpNextBanner`（加载/空态提前 return null、标记已看的
      pending/done 态与二次点击拦截、mutateAsync 失败静默吞掉）、
      `WatchHistoryPanel`（单集/全剧两种数据源与标题切换、
      SxE 徽章仅全剧态显示、dayjs 中文相对时间+绝对时间格式化、
      删除确认对话框成功/失败两分支、非 Error 拒绝值兜底文案）、
      `SearchModal`（防抖搜索、加载/错误/无结果/字数不足四态、
      结果渲染的图片回退、加入待看清单按 localId 是否存在走不同
      API、已在待看清单/添加中/添加失败三态）。
- [x] **踩坑**：`vi.useFakeTimers()` 若不带
      `{ shouldAdvanceTime: true }`，`waitFor()` 内部轮询依赖真实定时器
      推进会直接卡死，表现为一个模糊的 ~5s vitest 超时错误而不是明确的
      断言失败——只要测试里同时要固定系统时间又要用 `waitFor`/异步
      flush，就必须带上这个选项。
- [x] **踩坑**：模块级 `vi.fn()` 作为被 mock 的 hook 返回值时，
      不会在测试间自动清空调用记录（本项目未配置全局
      `clearMocks`）——前一个测试里对 `mockNavigate`/`mutateAsync` 等的
      调用记录会污染下一个测试的断言，必须在 `beforeEach` 里手动
      `mock.mockClear()`。
- [x] **踩坑**：`ConfirmDialog`/`SlidingPanel` 的关闭动画走
      `AnimatePresence`，在 jsdom 下不会可靠地真正卸载 DOM（与批次 4
      `TraktProgressBar` 的 `animate` prop踩坑同源）——断言取消/删除
      成功时不要等 DOM 消失，改为断言产生的副作用（mutateAsync 是否
      被调用、onDeleted 是否触发）。
- [x] **踩坑**：JSX 里相邻的文本表达式（如
      `{year}{" · "}<span>...</span>`）会被拼成同一个文本节点
      （"2020 · "），不是独立的 "2020"——`getByText` 精确匹配会失败，
      需要改用正则子串匹配。
- [x] **踩坑**：lucide 图标的内部注册名可能和导出名不同——`Loader2`
      渲染的 class 是 `lucide-loader-circle`（不是 `lucide-loader-2`），
      `MoreVertical` 内部也重导出自 `ellipsis-vertical`；断言前先读图标
      源码确认实际 class。
- 验收：新建 4 个测试文件（37 个测试）。`EpisodeGrid` 100%、
  `UpNextBanner` 90%、`WatchHistoryPanel` 97.2%、`SearchModal` 93.7%——
  本轮 branch 覆盖率增幅最大（+6.3pp）的一批，因为这几个都是带交互
  状态的组件。web 整体 53.94%→**58.96%** stmts / 45.89% branch /
  59.21% funcs / 59.18% lines。

### 批次 9 — 中型组件/页面 B：`TopNav`/`DiscoverPage`/`CalendarPage`（约 1243 行）[x]

- [x] `TopNav`（导航高亮逻辑 `isNavActive`、用户名双处显示、搜索弹窗开关、
      Now Playing 触发按钮 Trakt/Jellyfin 双数据源、watching 会话结束时
      的 invalidateQueries 副作用）、`DiscoverPage`（媒体类型/内容 tab
      切换驱动 `useDiscover` 重新请求、待看列表 toggle 按 localId 有无
      走不同 API、图片回退占位、trending 排名徽章仅在 trending tab 显示）、
      `CalendarPage`（`groupCalendarData` 按 episode 自身 airDate 分组+
      排序、"今天/明天/昨天/格式化日期"四态标题、日期选择器 prev/next
      边界禁用、dots+溢出徽章、结局徽章、`CalendarEpisodeArtwork` 三级
      图片回退链+未播出遮罩）。
- [x] **踩坑**：jsdom 完全没有实现 `Element.prototype.scrollIntoView`，
      任何调用它的组件（`CalendarPage` 的日期选择器自动滚动）会直接
      抛错，必须手动 stub：`Element.prototype.scrollIntoView = vi.fn()`。
- [x] **踩坑**：同一个字面量（如"今天"）如果既是常驻按钮文案又是动态
      状态值，两者同屏时 `getByText` 会因为多个匹配而失败——需要改用
      class/结构限定查询范围到具体元素。
- [x] **踩坑**：断言组件内部会对列表/同日项目排序时，测试 fixture 要
      故意按乱序声明，这样断言才能证明排序真的发生了，而不是恰好和
      输入顺序一致而蒙混过关。
- [x] **踩坑**：图片回退链（stillPath→backdrop→占位图，通过 onError
      触发）测试必须用只有一个条目的 fixture——多条目场景下，"移除"了
      一张图片后 `container.querySelector("img")` 仍会命中其它条目
      未受影响的图片，导致断言看起来通过了实际上什么也没验证。
- 验收：新建 3 个测试文件（36 个测试）。`TopNav` 84.8%、`DiscoverPage`
  80%、`CalendarPage` 93.9%——本轮分支覆盖率增幅再创新高（+7.2pp），
  `CalendarPage` 尤其分支密集（几十个日期/格式化分支）。web 整体
  58.96%→**64.69%** stmts / 53.13% branch / 64.52% funcs / 64.91%
  lines。

### 批次 10 — 大型组件/页面 A：`NowPlayingPopup`/`EpisodeInfoCard`/`LoginPage`（约 1661 行）[x]

- [x] `NowPlayingPopup`（导出的纯函数 computeRemainingMinutes/
      computeProgressPct/formatSeasonEpisode 独立单测、加载骨架/Trakt
      数据体/Jellyfin 数据体三态、poster 加载失败回退+切换剧集时重置
      错误态、每分钟 tick 倒计时、点击外部关闭排除 trigger 按钮、
      Jellyfin 标记已看成功后延迟 1s 关闭弹窗）、`EpisodeInfoCard`
      （翻译标题优先+原标题去重显示、首播日期/时长/导演等元信息含
      "未知"兜底、导演 1-2 人直接拼接·3+人用"等 N 人"归纳、简介兜底、
      genre 最多显示 3 个、外部链接按 ID 是否存在显示、标记已看→
      日期选择器两步流程、取消观看单条直接删/多条走选择器二次确认、
      Jellyfin 删除文件成功/失败 toast）、`LoginPage`（几乎纯静态营销页 + `?auth=success` 时重定向到 /tv-shows 的唯一副作用）。
- [x] **踩坑**：`setInterval` 回调里的 `setState`（倒计时 tick）发生在
      React 事件处理函数之外，推进假定时器必须包一层 `act()`，否则
      下一条断言执行时状态更新还没 flush 到 DOM。
- [x] **踩坑**：`EpisodeInfoCard` 一个页面里有 4 个独立
      `ConfirmDialog`，AnimatePresence 关闭动画在 jsdom 不会真正卸载
      （批次 8 同款踩坑）——当两个不同弹框恰好用了相同的确认按钮文案
      （如都是"删除"）时会同时出现在 DOM 里，`getByRole` 直接报
      "找到多个元素"；解法是先用弹框自己独有的标题文字定位，再
      `within(title.closest(".rounded-2xl")).getByRole(...)` 把点击
      范围限定到那一个弹框。
- [x] **踩坑**：按钮的 `aria-label` 会静默覆盖它的可访问名，即使可见
      文字不同——`EpisodeInfoCard` 主操作按钮可见文字是"标记已观看"
      （markWatchedShort），但 `aria-label` 却是"标记为已观看"
      （markWatched），恰好和 DateTimePickerModal 确认按钮的可见文字
      撞在一起，导致 `getByRole("button", {name})` 误判为两个匹配；
      断言前要先确认源码里有没有 `aria-label` 覆盖，不能只看可见文字。
- 验收：新建 3 个测试文件（37 个测试）。`NowPlayingPopup` 93.7%、
  `EpisodeInfoCard` 88.5%、`LoginPage` 100%。web 整体 64.69%→
  **70.10%** stmts / 59.97% branch / 68.74% funcs / 70.48% lines。

### 批次 11 — 大型组件/页面 B：`MovieDetailPage`/`JellyfinTab.tsx`/`HeroSection`（约 2293 行）[x]

- [x] `JellyfinTab`（纯展示组件，无自身 hook，全部数据走 props——
      URL/API Key 输入框、媒体库勾选表、自动删除总开关、待删除队列/
      排除列表/删除历史三个列表的 loading/empty/populated 三态与整剧/
      分季/无 scope 三种归属文案、立即删除确认弹框）、`HeroSection`
      （标题翻译优先级、poster 加载失败回退、watched 徽章条件显示、
      4 个外部链接按 ID 存在与否显示、reset/history/forceSync/
      watchlist 四个动作按钮按 prop 是否传入显示、总进度百分比与
      每季进度状态文案三态：全部看完/未开始/部分观看、season 0 特判为
      "Specials"）、`MovieDetailPage`（资料/历史两个 tab、观看次数/
      累计时长/最近观看三个指标含未观看兜底、历史记录去重逻辑、
      标记观看→日期选择器两步流程、待看列表加入/移出成功 toast、
      Jellyfin 删除文件成功 toast、路由 id 非法时向下游 hook 传 0）。
- [x] **踩坑**：`MovieDetailPage` 的 tab 切换用
      `<AnimatePresence mode="wait">`——不同于批次 8/10 遇到的"关闭动画
      不清场导致旧内容残留"，`mode="wait"` 会让*新* tab 的内容在旧
      tab 退场动画播完之前根本不挂载，而 jsdom 里退场动画永远不会
      "播完"，导致切换 tab 后新内容永远不出现。解法是整个测试文件
      级别 mock 掉 `framer-motion`：用一个 `Proxy` 覆盖 `motion.*`
      的任意标签（`Button` 用 `motion.button`、卡片类组件用
      `motion.div`……不需要逐个枚举），`AnimatePresence` 直接透传
      children。
- [x] **踩坑**：两个兄弟元素复用同一个翻译 key 时（比如某个 tab
      按钮和它对应 section 自己的标题都写的
      `t("watchHistory.showTitle")`），断言前不能凭印象猜文案——
      猜错了会表现成"完全找不到元素"（0 匹配）而不是"找到多个"，
      第一眼看起来像是完全不同的 bug，必须重新去 zh-CN.json 里查
      实际值。
- 验收：新建 3 个测试文件（50 个测试）。`JellyfinTab` 100%、
  `HeroSection` 100%、`MovieDetailPage` 88.5%——全计划分支覆盖率
  增幅最大的一批（+9.3pp）。web 整体 70.10%→**75.91%** stmts /
  69.32% branch / 73.69% funcs / 76.62% lines。

### 批次 12 — 超大型页面：`ListsPage`/`CollectionPage`（约 2036 行）[x]

- [x] 体量与 api 侧 `syncMovies`/`syncUserCollection` 相当，通读后判断
      两个文件结构足够独立（各自的 hook 集合、modal、卡片组件均不
      共享），未再拆子批次，一次性完成。`ListsPage`（列表侧栏
      loading/empty/populated 三态、隐私徽章三种取值+未知值兜底、
      选中/取消选中、新建列表弹框的必填校验/隐私选择/成功关闭/
      背景点击关闭/点击内容不关闭、条目网格 4 种链接×海报组合、
      移出条目、删除列表二次确认流程）、`CollectionPage`（远端容量
      条百分比钳制在 100/清理按钮 70% 阈值门槛、清理最旧条目三种
      结果 toast——部分失败/成功释放/已在目标以下、清空远端成功/
      失败 toast、媒体类型筛选三态、收藏卡片鼠标悬停显示移除按钮
      及二次确认、剧集详情弹框季号排序与集数补零及规格徽章、电影
      规格弹框六个字段及无数据兜底）。
- [x] **踩坑**：`CollectionCard` 的悬停移除按钮容器只是靠
      `opacity`/`pointerEvents` 切换可见性，并不是条件挂载——只要
      网格里有条目它就一直在 DOM 里。这意味着一旦同一屏里同时有
      卡片网格和一个详情弹框（弹框的关闭按钮也是同一个 lucide `X`
      图标/class），`container.querySelector(".lucide-x")` 会先命中
      卡片自己的移除触发按钮而不是弹框的关闭按钮——修复方式是先用
      弹框独有的副标题文案定位到 `.closest('[role="presentation"]')`
      再在其内部 `querySelector`，而不是全局找第一个同 class 元素。
      其余分支缺口集中在两类低价值场景，延续批次 9-11 定下的"不做
      穷举"原则未继续补：`x ?? fallback`/`x ?? []` 防御性兜底分支在
      现有渲染路径里本就不可达，以及 `formatBadge`/
      `CollectionMovieModal` 字段表里那几条很长的分辨率/HDR/音频
      三元链只挑了几组有代表性的取值组合，未逐分支穷举。
- 验收：新建 2 个测试文件（73 个测试）。`ListsPage` 100% stmts/funcs/
  lines（92.1% branch）、`CollectionPage` 100% stmts/funcs/lines
  （84.15% branch）。web 整体 75.91%→**81.21%** stmts / 78.23% branch
  / 80.29% funcs / 82.27% lines。

### 批次 13 — 超大型页面：`settings/BackupTab.tsx`（1488 行，web 侧最大单文件）[x]

- [x] 通读后判断体量虽大但结构高度重复（GDrive/OneDrive 两张几乎
      copy-paste 的云盘卡片、WebDAV/S3 两张几乎 copy-paste 的字段表单卡
      片），且是纯受控组件（约 40 个 props 全部由父组件 `SettingsPage`
      持有 state，自身仅有云端文件列表/恢复流程用到局部 `useState`），
      未再拆子批次，一次性完成。覆盖：云盘 disabled 提示的 provider 名称
      拼接、GDrive/OneDrive 设备码授权发起→轮询→成功/过期/拒绝/网络失败
      五态、断开连接即使远端 revoke 失败也照常本地断开、WebDAV/S3
      表单必填校验与保存成功/失败（含无 message 的兜底文案分支）、定时
      备份下拉框、立即备份的成功计数/整体失败/请求本身失败三态+历史
      记录静默刷新、备份历史列表三态（文件名/错误信息/均无时的占位符）、
      云端文件列表加载三态、恢复确认弹框全流程（确认/取消/失败/无
      message 兜底）与全屏恢复完成遮罩。
- [x] **踩坑**：WebDAV 和 S3 两张卡片不像 GDrive/OneDrive 那样有
      `configured` 开关控制是否渲染——两者永远同时出现在屏幕上，且
      "保存并测试"（两张卡片保存按钮共用同一个翻译 key）、"••••••••"
      （密码框和密钥框共用同一个占位符字符串）从第一个测试开始就是
      裸查询即报"找到多个元素"——解法是写一个 `cardFor(headingText)`
      helper，从"WebDAV"/"S3"标题文字向上找两层 `.parentElement` 定位到
      该卡片自己的容器 div，后续查询都 `within()` 限定在这个范围内。
- [x] **踩坑**：恢复确认弹框的确认按钮文案和每个文件行自己的"恢复"
      按钮复用同一个翻译 key，弹框描述文案里又把文件名整个拼进一句话
      当子串——弹框打开后这两处都会和文件行本身的内容产生撞车；解法是
      先用弹框自己独有的标题文字定位到 `.closest(".hud-panel-strong")`
      （`ConfirmDialog` 卡片的 class），后续查询都限定在这个弹框范围内。
- [x] **踩坑**：GDrive/OneDrive 的设备码轮询是
      `setInterval(async () => {...}, ms)`——用普通
      `vi.advanceTimersByTime` 推进定时器不会等待回调内部
      `await api.backup.gdrivePoll(...)` 这个异步调用真正落地；必须用
      `await vi.advanceTimersByTimeAsync(ms)`（此前 SearchModal 的防抖
      测试已验证过的写法）才能让轮询结果被正确观察到。
- [x] **踩坑**：当一次成功的恢复/断开操作在同一个 state 批次里，既把
      某个 prop 驱动的弹框 `isOpen` 翻转为 false、又同时把它的
      `isLoading` 清空时，AnimatePresence 在 jsdom 下退场动画永远播不完
      的老问题会把该弹框最后一次渲染的 props（`isLoading` 仍为 true）
      冻结在 DOM 里——断言时改为验证产生的副作用（恢复流程的全屏
      "恢复完成"遮罩本身不受 AnimatePresence 控制，可以可靠断言），
      而不是断言那个已经冻结的按钮 disabled 状态会恢复。
- 验收：新建 1 个测试文件（65 个测试）。`BackupTab.tsx` 98.67% stmts /
  100% lines / 97.56% branch / 97.14% funcs——本计划所有大型文件里
  最高的成绩。web 整体 81.21%→**86.22%** stmts / 81.32% branch /
  83.13% funcs / 87.57% lines，765 个测试。**至此 web 侧 13 个批次
  全部完成，N3-T02 web 部分收官。**

## 验收标准

- 批次 1-3（api.ts/hooks/push.ts）优先执行，是本轮杠杆最高的基础设施
  层，预期本身就能把 web 整体覆盖率从 25.45% 推高一个明显台阶。
- 批次 4-7（小型组件+stats+JellyfinStatsPage）体量小、模式统一，适合
  连续执行。
- 批次 8-13 体量递增，允许跨会话延续执行；批次边界已在本计划中明确，
  后续任何一次执行都可以直接从某个批次开始。
- 每个子批次：web tsc 全绿、全套件测试全绿（只增不减）、独立 commit。
- 不在任何测试里触碰真实后端 API/浏览器真实通知权限；`lib/push.ts` 的
  测试验证的是订阅状态机分支正确性，不会真的请求浏览器通知权限。
- 阈值上调后 `pnpm test:coverage`（workspace 级）5/5 tasks successful，
  不把 ratchet 调到实际值之上造假红。
