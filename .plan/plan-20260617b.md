# Plan 20260617b — 第二輪代碼審查 15 個發現修復

## Context

`/code-review` 對 v0.50.4→v0.50.7（上一輪 11 個修復後）進行整體審查，找出 15 個問題。
按嚴重度分 3 批修復，共涉及 6 個檔案。

## Batch 1 — HIGH（4 個問題，2 檔案）

| #    | 檔案                                               | 問題                                                                                                                                              |
| ---- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1-1 | `apps/web/public/sw.js` lines 7–16                 | `.catch(()=>{})` 在 skipWaiting 之前，install 失敗仍激活 SW（空 cache），offline 導航返回 Response.error()。移除 catch 讓 install 失敗保留舊 SW。 |
| B1-2 | `apps/web/public/sw.js` lines 20–27                | activate: `clients.claim()` 如果 `caches.delete` 拋錯會被跳過。在 chain 加 `.catch(()=>{})` 保證 claim 總執行。                                   |
| B1-3 | `apps/web/public/sw.js` line 42                    | navigate fetch handler 的 `cache.put` 是 floating promise，quota 等錯誤靜默丟棄。加 `.catch(()=>{})` 防止 UnhandledRejection。                    |
| B1-4 | `apps/web/src/pages/SettingsPage.tsx` line 118–122 | enable 路徑失敗時 `syncPushState()` 返回 true（瀏覽器有訂閱但後端無記錄），UI 誤顯示已啟用。改為區分 enable/disable 方向分別處理。                |

## Batch 2 — MEDIUM（4 個問題，2 檔案）

| #    | 檔案                                           | 問題                                                                                                                               |
| ---- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| B2-1 | `apps/web/src/lib/push.ts` line 73             | `unsubscribe()` 拋錯（push service 410）→ 永久卡死（每次 enablePush 卡在同點）。catch unsubscribe error 繼續 subscribe。           |
| B2-2 | `apps/web/src/lib/push.ts` line 69             | `new Uint8Array(k as ArrayBuffer)` cast 不準確（Chrome 返回 Uint8Array）。改用 `k instanceof Uint8Array ? k : new Uint8Array(k)`。 |
| B2-3 | `apps/api/src/jobs/airing-reminders.ts` 84–115 | ① `distinctShows` Set 在單集分支前無條件建立。② 多劇 title 顯示 `+N` 但 body 只列 4 集，count 不一致。加 overflow 指示器。         |
| B2-4 | `apps/api/src/jobs/airing-reminders.ts` 118    | `Promise.all(userSubs.map(sendPush))` 若任一 reject，整個用戶 batch 中斷。每個 sendPush 加 `.catch()` 防止鏈斷裂。                 |

## Batch 3 — LOW（2 個問題）

| #    | 檔案                                                  | 問題                                                                                                |
| ---- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| B3-1 | `apps/web/src/pages/SettingsPage.tsx` line 90, 96     | `syncPushState` 缺少 `useCallback`，useEffect deps 未完整列出。加 `useCallback([], [])` 並修 deps。 |
| B3-2 | `apps/api/src/__tests__/notifications-routes.test.ts` | mock `where()` 忽略 ne() 過濾器，測試無法真正驗證 cap 排除邏輯。加說明注解。                        |

## 不修改項目

- **disablePush silent when isPushSupported=false**（push.ts:86）：架構限制，device B 無法取得 device A 的 endpoint。現有 404/410 auto-prune 可最終清理。
- **TOCTOU 並發 cap bypass**（notifications.ts:34）：需 DB 層事務或 advisory lock，超出本次 patch 範圍，已在 B2-1 的 `ne()` 縮小窗口。
- **User B takes User A endpoint → false 429**：重新分析確認行為正確（B 取 A 的 endpoint 使 B 的總數 +1，若 B 已有 10 個則應拒絕）。

## 執行順序

1. Batch 1（sw.js + SettingsPage catch 修正）→ 跑測試 → commit
2. Batch 2（push.ts + airing-reminders.ts）→ 跑測試 → commit
3. Batch 3（useCallback + 測試注解）→ commit
4. 更新 `requirement.md` + version bump + push
