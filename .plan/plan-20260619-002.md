# Plan 20260619-002 — 功能落地执行计划（F02–F12）

## 背景

基于 plan-20260619-001 功能路线图，按「逐个执行」策略落地。
F01（电影 Watch Again）✅ 已完成；F11（PWA）✅ 已完整实现（manifest + sw.js + 图标）。
从 F02 开始，每项功能完成后执行 minor bump 并推送双端。

当前版本：v0.51.3

---

## 版本映射

| 功能 | 描述                           | 目标版本 | 状态      |
| ---- | ------------------------------ | -------- | --------- |
| F01  | 电影 Watch Again 弹窗          | v0.51.x  | ✅ 已完成 |
| F02  | 播出提醒分级（首播/回归/季终） | v0.52.0  | ✅ 已完成 |
| F03  | 统计热力图 & 习惯分析          | v0.53.0  | ✅ 已完成 |
| F04  | 全局搜索 + 一键加入 Watchlist  | v0.54.0  | ✅ 已完成 |
| F05  | Up Next 接着看面板             | v0.55.0  | ✅ 已完成 |
| F06  | 我的评分写入 + 评分统计        | v0.56.0  | [ ]       |
| F07  | Jellyfin 正在播放联动          | v0.57.0  | [ ]       |
| F08  | 发现页（Trending / 推荐）      | v0.58.0  | [ ]       |
| F09  | 集级笔记                       | v0.59.0  | [ ]       |
| F10  | 自定义列表                     | v0.60.0  | [ ]       |
| F11  | PWA 安装支持                   | v0.51.x  | ✅ 已完成 |
| F12  | 历史数据导入                   | v0.61.0  | [ ]       |

---

## F02 · 播出提醒分级（v0.52.0）

用户可单独开关三类播出提醒：**首播**（S1E1）/ **回归季**（SxE1, x>1）/ **季终**（本季最后一集）。

### 任务清单

- [x] A1 DB migration 0012 — `user_settings` 新增 `notification_event_types TEXT`
- [x] A2 `airing-reminders.ts` — 事件类型检测 + 按用户设置过滤
- [x] A3 `settings.ts` 路由 — 读写 `notificationEventTypes`
- [x] A4 `SettingsPage.tsx` — 三类通知开关 UI
- [x] A5 i18n keys（zh-CN / en-US）

---

## F03 · 统计热力图（v0.53.0）

Stats 页新增 GitHub 风格 52×7 日历热力图 + 星期分布图。

### 任务清单

- [x] B1 `stats.ts` 路由 — `heatmap`（365天）+ `weekdayDistribution`
- [x] B2 `types` — `StatsOverview` 更新
- [x] B3 `WatchHeatmap.tsx` — 52×7 SVG 格
- [x] B4 `WatchPatterns.tsx` — 星期分布图
- [x] B5 `stats/index.tsx` 集成

---

## F04 · 全局搜索（v0.54.0）

顶栏搜索框调 Trakt 搜索 API，结果直接加 Watchlist。

### 任务清单

- [x] C1 `trakt.ts` — `searchShows` / `searchMovies` / `searchBoth`
- [x] C2 `routes/search.ts` 新建
- [x] C3 `index.ts` 路由注册
- [x] C4 `api.ts` 客户端
- [x] C5 `SearchModal.tsx` 组件
- [x] C6 `TopNav.tsx` 搜索入口
- [x] C7 结果 + Watchlist 操作
- [x] C8 i18n

---

## F05 · Up Next 面板（v0.55.0）

TVShowsPage 顶部横向滚动条带，聚合所有追剧下一集。

### 任务清单

- [x] D1 `GET /shows/up-next` 路由
- [x] D2 `UpNextItem` type
- [x] D3 `useUpNext()` hook
- [x] D4/D5 TVShowsPage 条带 UI + 一键标记

---

## F06 · 我的评分（v0.56.0）

写回用户评分到 Trakt，Stats 新增评分分布图。

### 任务清单

- [ ] E1 DB migration 0013 — `user_ratings` 表
- [ ] E2 `trakt.ts` 评分 API
- [ ] E3 `routes/ratings.ts`
- [ ] E4 全量同步拉取评分
- [ ] E5 `StarRating.tsx` 组件
- [ ] E6 三处详情页集成
- [ ] E7 Stats 评分分布
- [ ] E8 i18n

---

## F07 · Jellyfin 播放联动（v0.57.0）

读 Jellyfin Sessions，NowPlayingPopup 扩展 + 追平按钮。

### 任务清单

- [ ] F1 `jellyfin.ts` `getActiveSessions`
- [ ] F2 `GET /jellyfin/now-playing`
- [ ] F3 `useJellyfinNowPlaying` hook
- [ ] F4 `NowPlayingPopup` 扩展

---

## F08 · 发现页（v0.58.0）

Trending / Popular / Recommendations 卡片网格。

### 任务清单

- [ ] G1 `trakt.ts` discovery 方法
- [ ] G2 `routes/discover.ts`
- [ ] G3 路由注册
- [ ] G4 `DiscoverPage.tsx`
- [ ] G5 TopNav 导航项
- [ ] G6 `App.tsx` 路由
- [ ] G7 i18n

---

## F09 · 集级笔记（v0.59.0）

私人观后感，本地存储，可导出。

### 任务清单

- [ ] H1 DB migration 0014 — `user_notes` 表
- [ ] H2 `routes/notes.ts`
- [ ] H3 详情页 UI 集成

---

## F10 · 自定义列表（v0.60.0）

双向同步 Trakt 用户列表。

### 任务清单

- [ ] I1 DB migration 0015 — `user_lists` + `user_list_items`
- [ ] I2 `trakt.ts` 列表方法
- [ ] I3 `routes/lists.ts`
- [ ] I4 `ListsPage.tsx` + 导航

---

## F12 · 历史数据导入（v0.61.0）

JSON 格式导入与现有导出对称。

### 任务清单

- [ ] K1 `POST /history/import`
- [ ] K2 导入 UI
- [ ] K3 i18n
