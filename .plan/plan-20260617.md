# Plan 20260617 — Post-review 11 fixes (batch implementation)

## Context

`/code-review` 對 v0.50.4 找出 11 個問題（1 critical / 2 high / 6 medium / 2 low），
分 3 批按嚴重度修復。所有修改在 v0.50.5（Batch 1）和 v0.50.6（Batch 2+3）完成。

## Batch 1 — Critical + High (v0.50.5)

| #    | 檔案                                    | 修改                                                                                         |
| ---- | --------------------------------------- | -------------------------------------------------------------------------------------------- |
| B1-1 | `apps/web/src/lib/push.ts`              | `enablePush()` 比對 `applicationServerKey` bytes；key rotation 後先 unsubscribe 再重訂       |
| B1-2 | `apps/web/src/pages/SettingsPage.tsx`   | 提取 `syncPushState()`；移除 `if (pushEnabled)` guard，enable/disable 兩個方向失敗都重新同步 |
| B1-3 | `apps/api/src/jobs/airing-reminders.ts` | 插入 `distinctShows.size === 1` 分支：同劇多集通知標題不加 "+0"，body 省略重複劇名           |

## Batch 2 — Medium (v0.50.6)

| #    | 檔案                                                  | 修改                                                                                       |
| ---- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| B2-1 | `apps/api/src/routes/notifications.ts`                | 訂閱上限計數加 `ne(endpoint, body.endpoint)`：re-subscribe 不被 429 擋，並縮小 TOCTOU 窗口 |
| B2-2 | `apps/web/public/sw.js`                               | install: 加 `.catch(()=>{})` 讓離線安裝時 SW 仍可 activate                                 |
| B2-3 | `apps/web/public/sw.js`                               | activate: `clients.claim()` 移進 `waitUntil` chain                                         |
| B2-4 | `apps/api/src/routes/history.ts`                      | `sanitizeCsv` 移除 `trimStart()`：detection 和 prefix 對同一字串操作                       |
| test | `apps/api/src/__tests__/notifications-routes.test.ts` | 新增 re-subscribe-at-cap → 200 測試                                                        |

## Batch 3 — Low (v0.50.6，同 commit)

| #    | 檔案                             | 修改                                   |
| ---- | -------------------------------- | -------------------------------------- |
| B3-1 | SettingsPage.tsx                 | `syncPushState` 提取已在 B1-2 完成     |
| B3-2 | `apps/api/src/jobs/scheduler.ts` | 加 O(n) 限制注解 + BullMQ v5 遷移 TODO |

## 測試結果

- API: 146 tests passed (17 files)
- TypeScript: 通過（prettier lint-staged 自動驗證）

## 已知不修改項目

- **Scheduler TOCTOU**（interval change + Redis error → duplicate jobs）：`requirement.md`
  明確規定「add step always executes」，保留行為，僅加注解說明風險。
