# Plan 2026-07-13 — N3-T02 覆盖率回升（第三批：backup + jellyfin）

## 背景

N3-T02 前两批（2026-07-12）把 api 覆盖率从 22.4% 拉到 41.0%（stmts），补齐了
collection/lists/stats/ratings/notes/auth-middleware/system/trakt/discover/search/
img/auth 共 12 个路由文件，四个文件（trakt/discover/img/auth）达到 100%。

剩余零覆盖的两个文件是全仓库 **blast radius 最大**的代码：

- `services/backup.ts`（953 行，3.72%）+ `routes/backup.ts`（980 行，5.02%）——
  `restoreDatabase()` 会 `DROP SCHEMA public/drizzle CASCADE` 后重放 pg_dump，
  一旦回归漏检，恢复接口可能把生产库炸穿；四个云 provider（GDrive/OneDrive/
  WebDAV/S3）的上传/下载/清理全部无测试。
- `services/jellyfin.ts`（494 行，0%）+ `routes/jellyfin.ts`（570 行，0%）——
  `deleteJellyfinItem` / `autoDeleteJellyfinEpisode` 是全仓库唯一真删 Jellyfin
  媒体文件的调用点（job 层 `jellyfin-auto-delete.ts` 已有单测，但它依赖的这两个
  service 函数本身从未被直接测试过）。

这两块体量合计约 3000 行，是目前 api 端最大的单块缺口，价值明显高于继续测
web 页面组件。

## 执行原则

- **绝不联真实服务**：所有测试必须 mock `fetch`（Jellyfin/云 provider HTTP 调用）
  和 `child_process.spawn`（pg_dump/psql），任何情况下不允许在测试中触发真实
  网络请求或真实 shell 命令。
- **测试只读，不代表可以跑真实自动删除**：这批工作是给 `deleteJellyfinItem`
  等函数补单测（mock 调用），不涉及、也不会执行 `runJellyfinAutoDelete()`
  或任何针对生产 Jellyfin/数据库的真实操作（沿用项目既有纪律）。
- 复用已验证的 mock 模式：HTTP provider 用 `vi.stubGlobal("fetch", ...)`
  （参考 `img-routes.test.ts`/`auth-routes.test.ts`）；DB chain-mock 沿用
  `collection-routes.test.ts` 的 SelectBuilder/ChainBuilder 风格。
- 每个子批次独立验证（tsc + 全套件 + 覆盖率）+ 独立 commit，避免一次改动过大
  难以定位问题。

## 批次清单

### 批次 1 — `services/jellyfin.ts` 单测 ✅

- [x] `jellyfinFetch` + `fetchJellyfinLibraries`：`/Users` 空数组提前返回、
      `JELLYFIN_USER` 环境变量匹配用户 vs 回退第一个用户、`/Users`/`/Views`
      非 2xx 抛错。
- [x] 剧集查找链：`fetchJellyfinSeriesTmdbMap`、`findJellyfinSeasonIdBySeriesId`、
      `findJellyfinSeasonEpisodes`、`findJellyfinEpisode`（含库范围限定、
      provider-id 客户端过滤——`AnyProviderIdEquals` 在 Jellyfin 端不生效的
      既有约束）。
- [x] 电影查找：`fetchJellyfinMoviesTmdbMap`、`findJellyfinMovie`。
- [x] `deleteJellyfinItem`：成功/非 2xx 抛错——重点断言只 mock 不触发真实调用。
- [x] `getActiveSessions` + `autoDeleteJellyfinEpisode`（正在播放联动 + 自动删除
      判定的服务层部分）。
- [x] 统计四件套：`getJellyfinLibrarySummary`、`getJellyfinActivityLog`、
      `getJellyfinTopItems`、`getJellyfinPlayHeatmap`。
- [x] 54 个测试，全部用 URL 正则路由的 fetch mock（不依赖调用顺序，比严格
      `mockResolvedValueOnce` 链更抗重构）。覆盖率 0%→99.44% stmts / 87.97%
      branch / 100% funcs / 100% lines；剩余分支缺口是 `init?.headers` 缺省等
      可选链兜底分支，风险极低，不追。

### 批次 2 — `routes/jellyfin.ts` 单测 ✅

- [x] `getJellyfinConfig` 辅助函数（含 v1: 加密串损坏时 catch-all 返回 null，
      未配置视为 503 而非 500）+ `GET /now-playing`（剧集用剧集海报/电影用
      条目自身海报、tmdbId 无本地行时 localShowId/MovieId 为 null、service
      抛错时静默吞掉返回 null 而非 502——与其余端点行为不同）+ `GET/POST
/libraries`。
- [x] 单集/电影/季度详情查找三个 GET 端点 + 季度批量删除（`Promise.all` 全
      成功才报删除数，任一失败 502）+ 单条目删除。
- [x] 删除队列：`GET /delete-queue`、`DELETE /delete-queue/:id`、
      `excludeQueueEntry` 辅助函数（defer 写 `deferUntil` ≈7 天后、never 写
      null、剧集与电影两种队列条目分支）+ `/defer`、`/never`、`/now`
      （mock `../jobs/jellyfin-auto-delete.js` 的动态 import）三个 POST。
- [x] 删除排除列表：`GET/POST /delete-exclusions`（title 回退 show/movie/
      em-dash）、`DELETE /delete-exclusions/:id`。
- [x] `GET /delete-history` + 四个 `/stats/*` 端点（overview/activity/
      top-content/heatmap，含 activity 的自定义 limit 透传）。
- [x] 66 个测试（含一条补充：损坏加密 key 走 catch-all 503 而非 500）。
      覆盖率 0%→97.26% stmts / 90% branch / 100% funcs / 99.54% lines。
      api 整体因这两批（jellyfin 服务层+路由层）跳到 50.46%/42.79%/
      47.82%/51.13%（stmts/branch/funcs/lines）。

### 批次 3 — `services/backup.ts`：四个云 provider ✅

- [x] GDrive：`isGDriveConfigured`、device-flow 三部曲（start/poll/revoke）、
      `uploadToGDrive`/`listGDriveBackups`/`downloadGDriveFile`/`deleteGDriveFile`/
      `pruneOldGDriveBackups`。
- [x] OneDrive：对称覆盖 device-flow（含 >4MB 分片上传会话）+ 上传/列表/下载/
      删除/清理。
- [x] WebDAV：`ensureWebDAVFolder`、`uploadToWebDAV`、`listWebDAVBackups`、
      `downloadWebDAVFile`、`deleteWebDAVFile`（含 origin+目录防护——专门测了
      `%2F` 编码绕过尝试，确认按注释所说不解码直接比对，安全拦截）。
- [x] S3：`uploadToS3`、`listS3Backups`、`downloadS3File`、`deleteS3File`、
      `pruneOldS3Backups`（`downloadS3File` 对非 `mediadash-backups/` 前缀的
      key 客户端直接拒绝，零 fetch 调用）。
- [x] 70 个测试。`isGDriveConfigured`/`isOneDriveConfigured`/`startXDeviceFlow`
      的"已配置"分支依赖模块加载时读取的环境变量常量，用 `vi.resetModules()` + 动态 re-import 测试。覆盖率 3.72%→74.19% stmts / 71.63% branch /
      62.96% funcs / 75.93% lines；剩余缺口是批次 4 的 dumpDatabase/
      restoreDatabase（child_process，非 HTTP）。api 整体到 56.77%/47.57%/
      56.03%/57.4%。

### 批次 4 — `services/backup.ts`：数据库 dump / restore（最高风险）✅

- [x] `dumpDatabase()`：mock `child_process.spawn` 返回假 ChildProcess（真实
      `node:stream` PassThrough 当 stdout/stderr，让源码里真实的 `createGzip()`
      管道正常跑——测试拿到的是真压缩字节，用真实 `gunzipSync` 验证内容，
      不需要另外 mock zlib）——覆盖成功路径、pg_dump 非 0 退出码、空产出
      （<100 字节）判定、spawn 失败（ENOENT）、超时 kill 分支（`vi.useFakeTimers` + `advanceTimersByTimeAsync`）。
- [x] `restoreDatabase()`：非 gzip 数据拒绝（不 spawn）、gzip 但缺
      "PostgreSQL database dump" 签名拒绝（不 spawn）、成功时断言 DROP SCHEMA
      前置 SQL 确实排在重放 dump 内容之前写入 psql stdin（PassThrough 监听
      data 事件捕获真实写入字节，不是断言调用参数）、psql 非 0 退出码 reject
      附带 stderr、spawn 失败、超时 kill。
- [x] 13 个测试，全部一次通过（真实 stream/gzip 机制 + `await` 天然让出事件
      循环，不需要手动 `nextTick`/`setImmediate` 编排）。覆盖率 services/backup.ts
      74.19%→97.02% stmts / 80.76% branch / 95.06% funcs / 97.42% lines。
      api 整体到 58.81%/48.21%/60.22%/59.27%。

### 批次 5 — `routes/backup.ts` 单测 ✅

- [x] provider 凭据 CRUD：gdrive/onedrive 的 auth+poll+revoke+status（含
      GDrive re-consent 不带 refresh_token 时保留旧值的既有业务规则），
      webdav 的 PUT+status，s3 的 PUT+status（真实 encryptToken/decryptToken
      往返，未 mock 加解密层）。
- [x] 备份设置 `PUT/GET /settings`（scheduleHours 变更才触发 job 重新注册）。
- [x] `POST /trigger`：dump 失败时全部 provider 记 failed、单 provider 失败
      不阻断其余 provider、显式指定单个 provider 时即使未连接也会尝试
      （记录对应的"未连接"错误）、prune 失败不影响本次备份判定为成功。
- [x] `GET /runs`、`GET /files`（多 provider 聚合 + 单 provider 失败容错）、
      `DELETE /files`。
- [x] `POST /restore`（全仓库最危险的端点）：下载优先（文件不可达时
      dumpDatabase/uploadToX/restoreDatabase 全部零调用）→ 安全快照上传
      失败则中止（restoreDatabase 不会被调用）→ 成功时验证下载→快照→
      恢复的顺序 + `requiresRestart`/`safetyBackup` 响应体 → **`process.exit`
      必须提前 mock 成 no-op**（1.5s 后的 `setTimeout` 是真实定时器，测试里
      不这样做会有真实进程被杀掉的风险）→ 模块级并发锁：第二个请求进来时
      前一个还挂在 pending 的 download 上，断言 409，再放行第一个并等它
      结束，避免 `restoreInProgress` 状态泄漏到后续测试。
- [x] `runScheduledBackup` 包装函数：无调度配置时零调用、dump 失败不建
      run 记录、优先用户指定的 activeProvider、该 provider 未连接时回退到
      全部已连接 provider、单 provider 失败不抛出。
- [x] 59 个测试。过程中順手修了测试基础设施的两个坑：`ChainBuilder` 缺
      `.catch()`（源码有处 `.insert().values().catch()` 直接链式调用，
      `PromiseLike` 只保证 `.then()`）；`revokeGDriveToken`/
      `registerUserBackupJob` 忘记给默认 resolved value 导致 `.catch()`
      挂在 `undefined` 上炸掉。覆盖率 routes/backup.ts 5.02%→74.88% stmts /
      67.46% branch / 59.32% funcs / 79.94% lines；剩余缺口是
      `runScheduledBackup` 里 onedrive/s3 分支——和已验证的 gdrive/webdav
      走同一段编排代码，只换 provider，边际收益低，不追。api 整体到
      65.28%/53.89%/65.86%/65.91%。

### 批次 6 — 阈值 ratchet 收尾 + 对账

- [ ] 重跑 api 全套件 + 覆盖率，`vitest.config.ts` 阈值上调至新实际值下方。
- [ ] requirement.md：N3-T02 记录本批数字；顺手修正 N2-T03——写 stats 测试时
      发现 `routes/stats.ts` 其实已实现 `yearComparison`（年度对比）和
      `longestStreakDays`（最长连续记录），但 N2-T03 仍写"缺"，一并更正。
- [ ] README.md 计划索引补 plan-20260712.md（补记，此前遗漏）和本文件的行。

## 验收标准

- 每个子批次：api tsc 全绿、全套件测试全绿（当前基线 308，只增不减）、独立 commit。
- 全部完成后 api 覆盖率相对 41.0%/33.5%/38.2%/42.1% 基线有实质回升。
- 阈值上调后 `pnpm test:coverage` 仍绿，不允许把 ratchet 调到实际值之上造假红。
- 所有新测试对 `fetch`/`child_process.spawn` 全 mock，不触碰真实 Jellyfin/云
  provider/数据库。
