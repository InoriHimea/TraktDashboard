# Plan 20260619-001 — 功能扩展路线图（全量代码审查后续）

## 背景

2026-06-19 全量代码审查（plan-20260619）除发现 15 处 Bug 外，同时评估了当前产品断点和可扩展方向。
本计划将审查中提议的 11 项新功能按「价值 ÷ 工作量」排列，供后续迭代分阶段落地。

现有已实现功能（不重复计划）：

- N2-T04 电影 Watch Again 弹窗 ✅（MovieDetailPage.tsx:585 已实现）
- N2-T05 Web Push 播出提醒 ✅
- N2-T06 数据导出（CSV/JSON）✅

---

## 实现约定

- DB 变更需同步更新 `packages/db/src/schema.ts`、写迁移 SQL、登记 journal、生成 snapshot
- 每项新 API 路由需要同步更新 `apps/web/src/lib/api.ts` 客户端
- i18n key 需同步写入 `zh-CN.json` 和 `en-US.json`
- 版本策略：每个 Sprint 完成后 minor bump（功能），仅 patch 修复时 patch bump

---

## Sprint A — 高价值 · 低成本（可独立快速交付）

### F01 · 电影 Watch Again 弹窗 ✅ 已完成

已在 `MovieDetailPage.tsx:585` 实现，无需重做。路线图标记为已完成。

---

### F02 · 播出提醒分级（首播 / 回归季 / 季终）

**价值**：复用已有 Web Push + VAPID + SW 基础，边际成本极低；用户可分别开关三类通知，减少噪音。

**实现面**：

| #   | 组件                                                                              | 文件                                      |
| --- | --------------------------------------------------------------------------------- | ----------------------------------------- |
| A1  | DB：`userSettings` 新增 `notification_event_types TEXT`                           | `packages/db/src/schema.ts` + 迁移 `0012` |
| A2  | 事件检测：`airing-reminders.ts` 判断 S1E1（首播）、SxE1（回归）、最后一集（季终） | `apps/api/src/jobs/airing-reminders.ts`   |
| A3  | Settings API 支持读写 notificationEventTypes                                      | `apps/api/src/routes/settings.ts`         |
| A4  | Settings UI：三类通知开关（checkbox 组）                                          | `apps/web/src/pages/SettingsPage.tsx`     |
| A5  | i18n keys                                                                         | `zh-CN.json` / `en-US.json`               |

**Effort**：M（约 2–3 天）

- [ ] A1 schema + migration 0012
- [ ] A2 事件类型检测逻辑
- [ ] A3 settings 路由读写
- [ ] A4 Settings UI 开关
- [ ] A5 i18n

---

### F03 · 统计增强——观看热力图 & 习惯分析（N2-T03 部分）

**价值**：数据全在 `watchHistory`，纯后端聚合 + 前端新卡片；补全路线图 N2-T03 的视觉吸引力。

**实现面**：

| #   | 组件                                                                               | 文件                                         |
| --- | ---------------------------------------------------------------------------------- | -------------------------------------------- |
| B1  | Stats API：新增 `heatmap`（365 天 date→count）+ `weekdayDistribution`（0-6→count） | `apps/api/src/routes/stats.ts`               |
| B2  | Types：`StatsOverview` 更新                                                        | `packages/types/src/index.ts`                |
| B3  | `WatchHeatmap.tsx`：GitHub 风格 52×7 SVG 格                                        | `apps/web/src/pages/stats/WatchHeatmap.tsx`  |
| B4  | `WatchPatterns.tsx`：星期分布条形图                                                | `apps/web/src/pages/stats/WatchPatterns.tsx` |
| B5  | Stats 首页集成两个新卡片                                                           | `apps/web/src/pages/stats/index.tsx`         |

**Effort**：M（约 2–3 天）

- [ ] B1 stats 路由扩展（heatmap + weekdayDistribution）
- [ ] B2 types 更新
- [ ] B3 WatchHeatmap 组件
- [ ] B4 WatchPatterns 组件
- [ ] B5 stats index 集成

---

## Sprint B — 高价值 · 中等成本（核心产品升级）

### F04 · 全局搜索 + 一键加入（Search & Add）

**价值**：应用最大的产品断点——用户无法发现新内容，必须去 Trakt 网站操作后等同步。

**实现面**：

| #   | 组件                                                                                  | 文件                                              |
| --- | ------------------------------------------------------------------------------------- | ------------------------------------------------- |
| C1  | Trakt search：`searchShows(q)` + `searchMovies(q)` + `searchBoth(q)`                  | `apps/api/src/services/trakt.ts`                  |
| C2  | 路由：`GET /search?q=&type=show\|movie\|all`                                          | `apps/api/src/routes/search.ts`（新建）           |
| C3  | Index 注册路由                                                                        | `apps/api/src/index.ts`                           |
| C4  | API 客户端 `api.search.query(q, type)`                                                | `apps/web/src/lib/api.ts`                         |
| C5  | `SearchModal.tsx`：全屏或顶部抽屉，输入框 + 结果列表                                  | `apps/web/src/components/SearchModal.tsx`（新建） |
| C6  | `TopNav.tsx`：搜索图标 → 触发 SearchModal                                             | `apps/web/src/components/TopNav.tsx`              |
| C7  | 结果卡片：显示海报/标题/年份/类型，「+ 加入 Watchlist」按钮（复用 useAddToWatchlist） | SearchModal 内                                    |
| C8  | i18n                                                                                  | `zh-CN.json` / `en-US.json`                       |

**Effort**：M（约 3–4 天）

- [ ] C1 trakt.ts 搜索方法
- [ ] C2 routes/search.ts
- [ ] C3 路由注册
- [ ] C4 api.ts 客户端
- [ ] C5 SearchModal 组件（防抖 + 加载态 + 空结果）
- [ ] C6 TopNav 搜索入口
- [ ] C7 搜索结果 + Watchlist 操作
- [ ] C8 i18n

---

### F05 · "接着看 / Up Next" 跨剧面板

**价值**：`nextEpisodeId` 已在 `userShowProgress` 里算好；只需聚合成统一队列，是日常使用最高频入口。

**实现面**：

| #   | 组件                                                                                                                                         | 文件                                 |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| D1  | 路由：`GET /shows/up-next`（join userShowProgress + episodes + shows，过滤 nextEpisodeId != null + 未完结，按 lastWatchedAt DESC，limit 20） | `apps/api/src/routes/shows.ts`       |
| D2  | Types：`UpNextItem` type                                                                                                                     | `packages/types/src/index.ts`        |
| D3  | Hook：`useUpNext()`                                                                                                                          | `apps/web/src/hooks/index.ts`        |
| D4  | `TVShowsPage.tsx`：页面顶部新增「接着看」横向滚动条带                                                                                        | `apps/web/src/pages/TVShowsPage.tsx` |
| D5  | 条带卡片：剧名 + S×E× 标题 + 一键「标记已看」                                                                                                | D4 内联实现                          |

**Effort**：S–M（约 2 天）

- [ ] D1 /shows/up-next 路由
- [ ] D2 UpNextItem 类型
- [ ] D3 useUpNext hook
- [ ] D4/D5 TVShowsPage 条带 UI

---

### F06 · 我的评分写入 + 评分统计

**价值**：个人评分是 Trakt 核心数据，当前只读社区评分。新增后可解锁评分分布、最佳/最差榜等统计维度。

**实现面**：

| #   | 组件                                                                               | 文件                                             |
| --- | ---------------------------------------------------------------------------------- | ------------------------------------------------ |
| E1  | DB 新表 `user_ratings`（userId, mediaType, traktId, tmdbId, rating 1-10, ratedAt） | schema.ts + 迁移 `0013`                          |
| E2  | Trakt：`addRating`、`removeRating`、`getRatings(type)` → `/sync/ratings`           | `apps/api/src/services/trakt.ts`                 |
| E3  | 路由：CRUD `/ratings`                                                              | `apps/api/src/routes/ratings.ts`（新建）         |
| E4  | 全量同步时拉取评分                                                                 | `apps/api/src/services/sync.ts` triggerFullSync  |
| E5  | `StarRating.tsx`：1–10 交互星组件（hover 预览 + 点击确认）                         | `apps/web/src/components/StarRating.tsx`（新建） |
| E6  | 集成：EpisodeInfoCard / ShowDetailPage / MovieDetailPage 各加评分区域              | 三处页面                                         |
| E7  | Stats：`RatingDistribution.tsx` 评分分布图 + 更新 stats 路由含评分聚合             | stats/                                           |
| E8  | i18n                                                                               | `zh-CN.json` / `en-US.json`                      |

**Effort**：M–L（约 4–5 天）

- [ ] E1 DB schema + migration 0013
- [ ] E2 trakt.ts 评分 API
- [ ] E3 routes/ratings.ts
- [ ] E4 全量同步集成
- [ ] E5 StarRating 组件
- [ ] E6 三处详情页集成
- [ ] E7 stats 评分分布
- [ ] E8 i18n

---

## Sprint C — 中等价值 · 中等成本

### F07 · Jellyfin 正在播放联动

**价值**：把单向「删文件」升级为「播放中枢」——读 Jellyfin Sessions 显示当前播放状态，一键「追平进度」。

**实现面**：

| #   | 组件                                                                | 文件                                          |
| --- | ------------------------------------------------------------------- | --------------------------------------------- |
| F1  | `getActiveSessions()` → `/Sessions?ActiveWithinSeconds=60`          | `apps/api/src/services/jellyfin.ts`           |
| F2  | 路由：`GET /jellyfin/now-playing`                                   | `apps/api/src/routes/jellyfin.ts`             |
| F3  | `useJellyfinNowPlaying()` hook（polling 30s）                       | `apps/web/src/hooks/index.ts`                 |
| F4  | `NowPlayingPopup.tsx`：扩展显示 Jellyfin 播放项；「追平到此集」按钮 | `apps/web/src/components/NowPlayingPopup.tsx` |

**Effort**：M（约 2–3 天）

- [ ] F1 jellyfin.ts getActiveSessions
- [ ] F2 /jellyfin/now-playing 路由
- [ ] F3 useJellyfinNowPlaying hook
- [ ] F4 NowPlayingPopup 扩展

---

### F08 · 发现页（Trending / 个性化推荐）

**价值**：「下一部看什么」是 Trakt 类应用的第二核心场景，当前完全缺失。

**实现面**：

| #   | 组件                                                                       | 文件                                          |
| --- | -------------------------------------------------------------------------- | --------------------------------------------- |
| G1  | Trakt：`getRecommendations(type)`、`getTrending(type)`、`getPopular(type)` | `apps/api/src/services/trakt.ts`              |
| G2  | 路由：`GET /discover/shows`、`/discover/movies`、`/discover/trending`      | `apps/api/src/routes/discover.ts`（新建）     |
| G3  | Index 注册                                                                 | `apps/api/src/index.ts`                       |
| G4  | `DiscoverPage.tsx`：Tab 切换（推荐/热门/流行）+ 卡片网格 + 加 Watchlist    | `apps/web/src/pages/DiscoverPage.tsx`（新建） |
| G5  | `TopNav.tsx`：导航栏加「发现」入口                                         | `apps/web/src/components/TopNav.tsx`          |
| G6  | `App.tsx`：注册路由 `/discover`                                            | `apps/web/src/App.tsx`                        |
| G7  | i18n                                                                       | `zh-CN.json` / `en-US.json`                   |

**Effort**：M（约 3–4 天）

- [ ] G1 trakt.ts discovery 方法
- [ ] G2 routes/discover.ts
- [ ] G3 路由注册
- [ ] G4 DiscoverPage
- [ ] G5 TopNav 导航项
- [ ] G6 App.tsx 路由
- [ ] G7 i18n

---

## Sprint D — 低优先级（锦上添花）

### F09 · 集级笔记（Episode Notes）

私人观后感，存本地不上传 Trakt，支持导出。

| #   | 组件                                                                                     | 文件                                   |
| --- | ---------------------------------------------------------------------------------------- | -------------------------------------- |
| H1  | DB 新表 `user_notes`（userId, episodeId?, movieId?, content TEXT, createdAt, updatedAt） | schema.ts + 迁移 `0014`                |
| H2  | 路由：GET/POST/DELETE `/notes/episode/:episodeId`、`/notes/movie/:movieId`               | `apps/api/src/routes/notes.ts`（新建） |
| H3  | EpisodeInfoCard / MovieDetailPage：可折叠笔记 textarea                                   | 两处详情页                             |

**Effort**：S–M（约 2 天）

- [ ] H1 DB + migration 0014
- [ ] H2 routes/notes.ts
- [ ] H3 UI 集成

---

### F10 · 自定义列表（Custom Lists）

双向同步 Trakt 用户列表（`/users/me/lists`）。

| #   | 组件                                                            | 文件                      |
| --- | --------------------------------------------------------------- | ------------------------- |
| I1  | DB：`user_lists` + `user_list_items` 两表 + 迁移 `0015`         | schema.ts                 |
| I2  | Trakt：getLists/createList/addListItem/removeListItem/syncLists | `services/trakt.ts`       |
| I3  | 路由：CRUD `/lists` + `/lists/:id/items`                        | `routes/lists.ts`（新建） |
| I4  | `ListsPage.tsx` + TopNav                                        | web/                      |

**Effort**：L（约 5–7 天）

- [ ] I1 DB + migration 0015
- [ ] I2 trakt.ts 列表方法
- [ ] I3 routes/lists.ts
- [ ] I4 ListsPage + 导航

---

### F11 · PWA 安装支持（N4-T01）

扩展现有 `sw.js`（已用于 Push），补全 manifest + 主屏安装。

| #   | 组件                                                                     | 文件                            |
| --- | ------------------------------------------------------------------------ | ------------------------------- |
| J1  | `manifest.json`（name, icons 512/192, display: standalone, theme_color） | `apps/web/public/manifest.json` |
| J2  | `index.html` 加 `<link rel=manifest>` + `theme-color` meta               | `apps/web/index.html`           |
| J3  | SW precache 壳页（扩展现有 sw.js install event）                         | `apps/web/public/sw.js`         |

**Effort**：S（约 0.5 天）

- [ ] J1 manifest.json
- [ ] J2 index.html 链接
- [ ] J3 SW 壳页 precache

---

### F12 · 历史数据导入（Import）

与现有 CSV/JSON 导出对称，从文件还原观看历史，便于迁移和灾备。

| #   | 组件                                                                                 | 文件                             |
| --- | ------------------------------------------------------------------------------------ | -------------------------------- |
| K1  | `POST /history/import`：接收 JSON 体（与 export 格式对称），批量 upsert，触发 recalc | `apps/api/src/routes/history.ts` |
| K2  | `HistoryPage.tsx` 或 `SettingsPage.tsx`：文件上传 + 进度提示                         | web/                             |
| K3  | i18n keys                                                                            | `zh-CN.json` / `en-US.json`      |

**Effort**：M（约 2 天）

- [ ] K1 POST /history/import 路由
- [ ] K2 导入 UI
- [ ] K3 i18n

---

## 实施优先级汇总

| Sprint | 功能                  | 前置条件      | Effort | 预估工期 |
| ------ | --------------------- | ------------- | ------ | -------- |
| A      | F02 播出提醒分级      | 无            | M      | 2–3 天   |
| A      | F03 热力图统计        | 无            | M      | 2–3 天   |
| B      | F04 全局搜索          | 无            | M      | 3–4 天   |
| B      | F05 Up Next 面板      | 无            | S–M    | 2 天     |
| B      | F06 我的评分          | F05（可并行） | M–L    | 4–5 天   |
| C      | F07 Jellyfin 播放联动 | Jellyfin 已配 | M      | 2–3 天   |
| C      | F08 发现页            | 无            | M      | 3–4 天   |
| D      | F09 集级笔记          | 无            | S–M    | 2 天     |
| D      | F10 自定义列表        | 无            | L      | 5–7 天   |
| D      | F11 PWA 安装          | 无            | S      | 0.5 天   |
| D      | F12 数据导入          | 无            | M      | 2 天     |

**建议落地顺序**：F05 → F04 → F02 → F03 → F06 → F08 → F07 → F09 → F11 → F12 → F10

---

## 验收标准（Sprint A 示例）

- F02：首播/回归/季终三类事件被正确检测并分别通知；Settings UI 开关生效
- F03：Stats 页出现热力图（52×7 格），鼠标悬停显示日期和集数；星期分布图正确渲染
