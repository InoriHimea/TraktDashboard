# Plan 2026-07-05 — N5 路线图分阶段推进（T10 除外）

## 背景

2026-07-02 巡检后规划的 N5 路线图（`.plan/requirement.md`）共 9 项待办（T10 需 12.0
发布前手动开启自动删除才有意义，本轮明确排除）。用户要求「除了 T10，其他按优先级顺序
分阶段处理」。按优先级拆为三阶段：

- **阶段一（高）**：N5-T05 — 修复 `upsertRepeatableJob` 去重 bug
- **阶段二（中）**：N5-T02（立即删除按钮）→ N5-T03（删除前推送提醒）→ N5-T04（电影自动删除）
- **阶段三（低）**：N5-T06 → N5-T07 → N5-T08 → N5-T09

## 实施清单

### 阶段一 — N5-T05（已完成，`1ef6dd4`）

- [x] 诊断：当前 bullmq（5.78.0）`getRepeatableJobs()` 返回对象无 `.id` 字段，旧的
      按 `.id` 匹配去重逻辑永远匹配失败，每次进程重启都新增一份重复任务
      （生产已确认 airing-reminders、cleanup-cache 各 ×2）。
- [x] 改用 BullMQ `upsertJobScheduler`/`removeJobScheduler`（按 `schedulerId` 原子
      update-in-place），三个每日 cron 任务 + 用户 sync/backup 任务全部切换。
- [x] 新增 `purgeLegacyRepeatableJobs()`，`startScheduler()` 最前面一次性清空旧模式
      遗留的重复任务（安全：所有任务紧接着通过新 API 重新注册）。
- [x] 验证：`tsc --noEmit` 全绿 + API 测试 146/146（scheduler 模块在测试中被完整
      mock，内部实现改动不影响用例）。

### 阶段二 — N5-T02（本轮完成）

- [x] `apps/api/src/jobs/jellyfin-auto-delete.ts`：新增导出函数
      `deleteQueueEntryNow(userId, queueId)`，复用 Phase 2 的单条目删除逻辑
      （解析 seriesMap → `deleteJellyfinItem` → 写历史 → 删队列行），但只为这一条目
      拉取 seriesMap，不等次日定时任务批量处理。
- [x] `apps/api/src/routes/jellyfin.ts`：新增 `POST /jellyfin/delete-queue/:id/now`。
- [x] `apps/web/src/lib/api.ts` + `hooks/index.ts`：`deleteQueueNow` API 方法 +
      `useDeleteNowJellyfinDelete` mutation（成功后失效队列 + 历史缓存）。
- [x] `apps/web/src/pages/SettingsPage.tsx`：队列条目新增"立即删除"按钮，走
      `ConfirmDialog` 二次确认（不可撤销的破坏性操作，与 `ShowDetailPage.tsx`
      季度删除按钮同款交互）。
- [x] i18n：zh-CN/en-US 新增 `jellyfinDeleteNow*` 五个键。
- [x] 验证：`tsc --noEmit`（api + web）全绿；API 测试 146/146；前端 dev server 加载
      SettingsPage 无控制台/编译报错（沙箱环境无法连接生产 Postgres/Redis，与此前
      N5-T05 验证时遇到的同一网络限制一致，故无法做登录后的真实点击回归——仅能验证
      到「页面正常加载、无运行时错误」这一层）。

### 阶段二 — N5-T03、N5-T04（待做）

- [ ] N5-T03：删除前 Web Push 提醒——条目首次入队时推送"X 将于明天删除"。
- [ ] N5-T04：电影自动删除——`jellyfin_delete_queue.movie_id` 已预留，判定规则待定
      （如已看完 + 满 N 天）。

### 阶段三（待做）

- [ ] N5-T06：清理死代码 `SyncPage.tsx`
- [ ] N5-T07：设置页拆分页签
- [ ] N5-T08：显示语言改下拉框
- [ ] N5-T09：完成或放弃 N1-T02 RPC 迁移

## 验收标准

- 每个子任务独立 `tsc --noEmit` + 现有测试套件通过后再进入下一项。
- 涉及 Jellyfin 真实删除的功能，验证过程只读或需用户明确确认后才可执行有损操作
  （沿用 `project_jellyfin_autodelete_rules` 的既有纪律）。
