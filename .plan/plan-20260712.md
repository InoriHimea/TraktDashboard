# Plan 2026-07-12 — N3-T02 覆盖率回升（第一批）+ N1-T04 对账

## 背景

N6 收官后路线图上优先级最高的遗留是 N3-T02（中）：覆盖率曾达 api 40% / web 28%，
被 N4/N5 未测新代码稀释到 ~22%，2026-07-07 重定基为诚实 ratchet。本轮开始把数字涨回去。

基线（2026-07-12 实测）：

- api：22.39% stmts / 16.77% branch / 22.38% funcs / 23.32% lines（阈值 21/16/22/22）
- web：21.96% stmts / 20.53% branch / 12.74% funcs / 22.77% lines（阈值 20/19/12/21）

零覆盖重灾区（按业务价值排序）：

- api `routes/collection.ts`（451 行）— add-only 归档语义是全项目最独特的业务规则，无任何测试
- api `routes/lists.ts`（436 行）、`routes/stats.ts`（432 行）
- api `routes/ratings.ts` / `notes.ts` / `middleware/auth.ts`（安全关键）/ `system.ts`
- web `hooks/index.ts`（891 行，5.7%）、`queryKeys.ts`（45%）、一批零覆盖小纯组件

## 批次清单

### 批次 1 — API 路由测试（复用 history-routes.test.ts 的 chain-mock 模式）✅

- [x] collection 路由：GET 列表/详情、POST 入库（本地归档 + Trakt 同步子集）、
      DELETE 双向传播；重点断言 add-only 语义（pull 不删本地）相关分支。31 个测试。
- [x] lists 路由：列表 CRUD + 条目增删 + 排序。27 个测试。
- [x] stats 路由：月度图表/热力图/习惯分析/评分分布等只读聚合端点的形状与边界。9 个测试。
- [x] 小件：ratings / notes CRUD；middleware/auth JWT 校验（无 cookie 401 /
      坏 token 401 / 好 token 注入 userId，真实 jose 签名往返而非 mock）；
      system 健康端点。共 30 个测试。

### 批次 2 — Web 端测试 ✅

- [x] queryKeys.ts 补到全绿（纯函数，12 个测试覆盖每个 factory + 家族前缀关系）。
- [x] 零覆盖小纯组件：StatCard / TopGenres / OverviewText / PendingDeleteBadge / Toaster
      （Toaster 需 mock framer-motion 的 AnimatePresence，否则退场动画在 jsdom 下
      不会同步卸载，dismiss 断言会假失败）。
- [x] notes / lists / ratings 核心 hooks（QueryClient wrapper 复用已有
      watchlist-and-season.test.tsx 模式，覆盖查询 + mutation 失效 key）。

### 批次 3 — 阈值 ratchet + 对账 ✅

- [x] 重跑两端 coverage，阈值上调至新实际值下方（api 33/26/30/35，
      web 24/20/17/25；stmts/branch/funcs/lines 顺序对应两份 config 里的字段名）。
- [x] requirement.md：N1-T04 打勾（对账发现 `__tests__/calendar.test.ts` 早已覆盖
      watched 标志、90 天窗口钳制、空结果、非法参数回退——即任务要求的全部内容）；
      N3-T02 记录本轮回升数字。

## 收尾发现

- `pnpm lint` 报了 20 个 no-undef/jsx-a11y 错误，核实后全部来自嵌套 git worktree
  `.claude/worktrees/eloquent-haslett-385558/`（`apps/web/public/sw.js` 等的副本）—
  根级 eslint ignores 里的路径模式未加 `**/` 前缀，不覆盖嵌套目录，导致这个无关的
  worktree 被一并扫描。已用 lint 的 JSON 输出核实本轮新增的全部 12 个测试文件
  errorCount 均为 0；该 ignores 配置缺口已作为独立后台任务拆出，未在本轮处理
  （不属于覆盖率工作范围，且不应顺手动 eslint.config.mjs）。

## 验收标准（全部达成）

- api + web tsc 全绿、全 workspace 测试全绿（api 161→258，web 121→160，均只增不减）、
  vite build 成功。
- 覆盖率两端均较基线有实质回升：api 22.4%→35.1% stmts（目标 ≥28%）、
  web 22.0%→25.5% stmts（目标 ≥25%）。
- 阈值上调后 `pnpm test:coverage` 全绿（两端 5/5 tasks successful，无假红）。

## 批次 4（回顾补记，跨 07-12/07-13 夜间完成，当时未建计划文件）✅

- [x] 同一会话延续，追加 trakt/discover/search/img/auth 五个路由测试文件，50 个测试：
      trakt（profile/stats/watching 成功与 502）、discover（trending/popular ×
      show/movie、TMDB 海报兜底 fulfilled/rejected、limit 钳制）、search（q 长度
      门槛、type=all 的 ceil/floor 拆分、TraktApiError 502 与非 Trakt 错误 500
      直通）、img（TMDB 图片代理缓存新鲜命中/过期回源/损坏数据清理/冷启动写
      缓存/上游非 200 透传/fetch 异常/代理转发）、auth（OAuth state 校验、
      token 交换失败、新老用户两条入库路径、/me 已登录与失效会话、/logout；
      signToken/verifyToken 走真实 jose 签名）。四个文件（trakt/discover/img/
      auth）均达 100% 语句覆盖。
- [x] api 覆盖率 35.1%→41.0% stmts（33.5%/38.2%/42.1% branch/funcs/lines），
      阈值同步上调至 39/32/36/40。
- [x] **流程缺口**：这批工作实际发生在本文件"验收标准全部达成"之后、下一批
      （plan-20260713.md）开始之前，当时未按惯例新建 `plan-20260713.md` 就直接
      commit 了——2026-07-13 写 plan-20260713.md 时发现遗漏，回顾补记于此。
