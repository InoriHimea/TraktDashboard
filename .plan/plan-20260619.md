# Plan 20260619 — 审查修复（Code Review R01–R15）

## 背景

全量代码审查（2026-06-19）发现 15 处问题，按严重度分三级。本计划覆盖全部修复，不涉及新功能。

## 执行原则

- P0 必须全部修复，不可降级
- 修复顺序：后端基础设施 → 后端业务逻辑 → 前端
- 每个修复单独可验证，不引入功能变化
- 密钥加密（jellyfinApiKey）需向后兼容现有未加密值

---

## 任务清单

### P0 — 数据破坏 / 丢失 / 安全

- [x] **R01** `autoDeleteJellyfinEpisode` 未按授权库范围限定删除范围
    - 文件：`apps/api/src/services/jellyfin.ts:107`
    - 修复：查找分集时用 `AncestorIds=<allowedLibraryIds>` 限制搜索范围，跳过整体 hasAutoDeleteLib 前置检查

- [x] **R02** 增量同步电影失败不回滚游标，永久丢失观看记录
    - 文件：`apps/api/src/services/sync.ts:519`
    - 修复：在电影 catch 块中同步更新 `failedEntryMinWatchedAt`（参考剧集块的 380-385 行模式）

- [x] **R03** Migration `0011_jellyfin_settings.sql` 未登记进 drizzle journal，`migrate()` 跳过
    - 文件：`packages/db/drizzle/meta/_journal.json`
    - 修复：添加 idx:11 journal 条目 + 生成 `0011_snapshot.json`

- [x] **R04** `runMigrations()` 失败被 catch 吞掉，服务带残缺 schema 继续启动
    - 文件：`apps/api/src/index.ts:73`
    - 修复：catch 块中 `process.exit(1)` 实现真正的 fail-fast

### P1 — 用户可见正确性

- [x] **R05** `air_date` 存全 ISO 时间戳，却与日期串做字符串比较，当天剧集约 24h 不计入
    - 文件：`apps/api/src/services/sync.ts:1273,1316` + `apps/api/src/routes/calendar.ts:37-39`
    - 修复：比较时用 `LEFT(air_date, 10)` 截断到日期

- [x] **R06** 开发回退密钥在非 production 可用（start 脚本未设 NODE_ENV）
    - 文件：`apps/api/package.json`
    - 修复：`start` 脚本改为 `NODE_ENV=production bun src/index.ts`

- [x] **R07** `jellyfinApiKey` 明文存储并原样返回前端
    - 文件：`apps/api/src/routes/settings.ts`
    - 修复：存储时加密（复用 `encryptToken`），GET 返回 `"***"` 掩码，PUT 收到 `"***"` 时跳过更新

- [x] **R08** SettingsPage 设置表单在窗口聚焦重取时覆盖未保存的编辑
    - 文件：`apps/web/src/pages/SettingsPage.tsx:71`
    - 修复：添加 `isDirty` 守卫，seed effect 仅在表单未脏时生效；或改为仅挂载时 seed（`useRef` 标记）

### P2 — 较窄正确性 / 体验

- [x] **R09** `useDeleteJellyfinItem` 删除后不失效缓存，删除按钮保留可二次点击
    - 文件：`apps/web/src/hooks/index.ts:391`
    - 修复：添加 `onSuccess` → `queryClient.invalidateQueries`

- [x] **R10** `loadJellyfinLibraries` 用存储凭据而非输入框的值，改了地址/Key 不保存就加载出错
    - 文件：`apps/web/src/pages/SettingsPage.tsx:154`
    - 修复：改为直接传当前 input 值到新端点 `GET /jellyfin/libraries?url=&apiKey=`（或 POST body）

- [x] **R11** Trakt 分页 Header 缺失时 `parseInt` 静默回退为 1，历史截断
    - 文件：`apps/api/src/services/trakt.ts:457`
    - 修复：加 `Number.isFinite` 校验 + 记录告警

- [x] **R12** Stats `monthlyAverage` 分子含电影、分母仅剧集月份
    - 文件：`apps/web/src/pages/stats/index.tsx:108`
    - 修复：从 API 拿电影月度数据或在前端合并计算

- [x] **R13** `JSON.parse(jellyfinAutoDeleteLibraryIds)` 无 try/catch，脏值导致 settings 500
    - 文件：`apps/api/src/routes/settings.ts:77`
    - 修复：包裹 try/catch，parse 失败返回 `[]`

- [x] **R14** `findJellyfinEpisode` 用剧级 TMDB id 匹配 Episode ProviderIds，多数环境无匹配
    - 文件：`apps/api/src/services/jellyfin.ts:45`
    - 修复：R01 的 AncestorIds 方案同时改为按库内搜索，已通过 R01 一并解决

- [x] **R15** `mark-watched` 返回的 `alreadyWatched` 是用户全局值而非当季值
    - 文件：`apps/api/src/routes/shows.ts:706`
    - 修复：加 `eq(watchHistory.episodeId, sql\`IN (season episode ids)\`)` 过滤条件

---

## 验收标准

1. 所有 P0 修复后本地 `pnpm test --run` 全绿
2. Jellyfin 自动删除仅在配置库中搜索（通过 AncestorIds 参数）
3. 增量同步电影失败后游标回滚到失败时间点前
4. 独立运行 `pnpm db:migrate` 能正确创建 jellyfin 列
5. API 启动时 migration 失败应 exit(1) 而不继续
6. 当天播出剧集当天出现在日历和进度统计中
7. Settings 加载后切走再切回，未保存的编辑不被覆盖
8. 删除 Jellyfin 文件后删除按钮消失
