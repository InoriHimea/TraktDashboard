# Plan 2026-07-15 — N3-T02 覆盖率回升第五批：四个大文件的拆解与批次规划

## 背景

plan-20260714 三批次完成后，api 覆盖率 65.28%→69.58%（stmts），并明确把四个
大文件留到"后续计划"：

- `services/sync.ts`（2328 行，25.27% stmts）——全仓库最大单文件，也包含全仓库
  最大的单个函数。已确认的顶层导出（共 14 个，行号/大致行数）：
    - `airedWithinForceRefreshWindow`（58，~61 行）
    - `getSyncStatus`（120，~8 行）
    - `triggerFullSync`（128，~189 行）
    - `triggerIncrementalSync`（318，~335 行）
    - `forceSyncShow`（654，**~673 行，全仓库最大函数**）
    - `getPostResetWatchedEpisodeIds`（1328，~13 行）
    - `computeWatchedEpisodes`（1342，~5 行）
    - `recalcShowProgress`（1348，~98 行）
    - `recalcMovieProgress`（1447，~33 行）
    - `syncMovies`（1481，~386 行）
    - `syncWatchlist`（1868，~130 行）
    - `syncRatings`（1999，~95 行）
    - `syncUserCollection`（2095，~214 行）
    - `resetStaleRunningSyncs`（2310，~18 行）
- `services/trakt.ts`（1058 行，35.46% stmts）——Trakt API 核心客户端。
  `getTraktClient()`（491-1057）内部先定义 `traktFetchRaw`/`traktFetch`
  共享辅助函数（处理鉴权/token 刷新/重试/分页头，通过 `providerFetch`），
  然后返回约 50 个方法：其中 4 个（`getShowDetail`/`getSeasons`/
  `getEpisodes`/`getEpisodeRating`）实现了与 `services/tmdb.ts`
  几乎一致的 stale-while-revalidate 缓存模式（已在 plan-20260714 批次 2
  验证过该模式），其余约 40 个是直接调用 `traktFetch(path, userId, params)`
  的薄封装（watchlist/search/discovery/ratings/lists/stats/collection/
  history/progress/watching），其中 `getHistory`/`getMovieHistory` 各自
  带 `while(true)` 分页循环读取 `X-Pagination-Page-Count` 头。token 刷新
  链路（`refreshToken`，316 行起）已有专门测试
  `trakt-token-refresh.test.ts` 覆盖，本轮不重复测。
- `routes/shows.ts`（809 行，23.11% stmts）——剧集进度核心路由，12 个端点
  （已通读全文）：`GET /progress`、`GET /up-next`、`GET /:id`、
  `GET /:id/seasons`、`GET /:showId/episodes/:season/:episode`、
  `POST .../watch`（内含一个 fire-and-forget 的 Jellyfin 自动删除 IIFE，
  错误被吞、不阻塞响应）、`GET .../history`（单集）、`GET /:showId/history`
  （整剧）、`DELETE /:showId/history/:historyId`、`POST /:showId/reset`、
  `POST /:showId/seasons/:season/mark-watched`、`POST /:showId/force-sync`。
- `routes/history.ts`（409 行，35.44% stmts）——已确认 `GET /`（35）和
  `GET /export`（110）已被 `history-routes.test.ts` 充分覆盖（分页/
  mediaType 过滤/CSV 与 JSON 导出/公式注入防护/换行转义），**唯一缺口是
  `POST /import`（196-409，~213 行，完全零覆盖）**：JSON 解析失败→400；
  entries 为空/超过 50000→400；按 show 标题/movie 标题批量预查（大小写
  不敏感，`lower()` 比较而非 `ilike` 避免通配符误伤）；按 showId 批量预
  加载 episodes 建立 `${showId}:${season}:${episode}` 缓存避免 N+1；单条
  处理时按 mediaType 分支解析、标题/season/episode 缺失或查无对应
  show/movie/episode → skip；去重键包含 watchedAt（null 与非 null 分别
  用 `isNull`/`eq` 处理，避免 null 时间戳互相误判重复）；单条处理异常
  被 catch 且计入 `errors`（最多记录 20 条）而不中断整批。

以上四个文件合计约 4600 行，体量远超此前任何一批（jellyfin+backup 两大板块
加起来才约 2400 行、耗时 6 个批次）。本计划的目的是先把它们拆解成可独立
执行、可独立验证的批次，其中 `services/sync.ts` 单独占据后半部分（批次
6-10），因为它的函数体量本身就接近甚至超过此前整个文件的规模
（`forceSyncShow` 一个函数 ~673 行，比 `services/backup.ts` 全文件还大）。

## 执行原则

- 沿用已验证的 mock 模式：路由测试整体 mock 依赖的 service 模块
  （`../services/sync.js`、`../services/trakt.js`、`../services/jellyfin.js`、
  `../services/tmdb.js`）+ DB chain-mock；service 测试 mock 更底层的
  `providerFetch`（`../lib/http.js`，已被 `p1-http.test.ts` 覆盖，本轮
  只关心业务分支不重复测 HTTP 层）。
- `services/trakt.ts` 的 `traktFetch`/`traktFetchRaw` 是 `getTraktClient()`
  内部闭包，无法单独 mock/导出——测试通过 mock `providerFetch` 的返回值
  间接驱动，鉴权/token 部分用一个"始终有效未过期"的 mock token 短路掉，
  避免重复测已经被 `trakt-token-refresh.test.ts` 覆盖的刷新逻辑。
- `routes/shows.ts` 的 `POST .../watch` 内的 fire-and-forget IIFE（不
  `await`，错误被空 catch 吞掉）：测试策略是——响应本身必须在 IIFE 完成
  前就返回 200（验证"never blocks the response"）；IIFE 内部分支（是否
  调用 `autoDeleteJellyfinEpisode`）通过请求后 `await` 一次
  `Promise.resolve()`/`setImmediate` 让微任务队列 flush 后再断言 mock
  调用情况来验证四个门槛条件（jellyfin 配置存在 + autoDeleteLibraryIds
  非空 + show 状态 ended/canceled + 进度 100% completed）分别生效。
- `services/sync.ts` 每个批次开始前，先针对该批次涉及的具体函数做一次
  完整源码通读（本计划目前只掌握函数边界/行号，尚未逐行读取内部实现），
  沿用此前"先读全文再写测试"的方法论；不在本计划阶段预先读完全部
  2328 行。
- 每个子批次独立验证（tsc + 全套件 + 覆盖率）+ 独立 commit；不触碰真实
  Trakt/Jellyfin/TMDB 服务或生产数据库。
- 若本计划执行到 `services/sync.ts` 部分（批次 6-10）跨会话延续，允许
  拆分成独立的后续 plan 文件（如 plan-20260716.md 起）承接，只要
  requirement.md 与本文件的批次勾选状态保持同步。

## 批次清单

### 批次 1 — `routes/history.ts`：`POST /import` ✅

- [x] JSON 解析失败 → 400 `{error: "Invalid JSON"}`。
- [x] 顶层数组 body 与 `{entries: [...]}` 包裹形式都能被识别；entries 为
      空数组 → 400 "No entries found"；超过 50000 条 → 400 "Too many
      entries"。
- [x] show/movie 批量预查：命中 → 解析成功；未命中 → 该条 skip（`title`/
      `translatedName` 的 SQL 层大小写不敏感匹配属于 DB 行为，mock 层面
      不重复验证，只验证代码对查询结果的分支处理）。
- [x] episode 预加载缓存：同一 show 下 season/episode 组合命中/未命中。
- [x] 去重逻辑：同一集两条记录（一条 watchedAt 为 null、一条带时间戳）
      被各自独立去重检查（各触发一次 DB 查询），不会互相误判为重复。
- [x] mediaType 不是 `episode`/`movie` → skip；episode 分支缺
      showTitle/seasonNumber/episodeNumber → skip；movie 分支缺
      movieTitle → skip。
- [x] 单条处理抛异常被 catch，计入 `errors`（验证第 21 条起不再继续
      push，即最多记录 20 条，但 `skipped` 仍按实际失败数计数），且不
      中断后续条目处理，返回的 `{ok, imported, skipped, errors}` 计数
      正确。
- [x] 额外验证：bare array body（不包 `{entries}`）也能被正确识别。
- 验收：`history-routes.test.ts` 新增 `describe("POST /import")`（17 个
  测试），mock DB 按 `.from(table)` 引用分派到 shows/movies/episodes/
  watchHistory 各自的 FIFO 结果队列（而非单一全局队列），避免要精确
  预判每次 select 调用顺序。history.ts 覆盖率 35.44%→**98.1%** stmts
  （98.63% lines / 85.12% branch / 100% funcs）。api 整体
  69.58%→**71.77%** stmts / 59% branch / 71.17% funcs / 72.8% lines。

### 批次 2 — `routes/shows.ts`：只读路由（progress/up-next/详情类）[ ]

- [ ] `GET /progress`：分页/搜索/过滤参数；`percentage` 计算正确性
      （含 totalEpisodes 为 0 的边界）。
- [ ] `GET /up-next`：每个未完成 show 返回下一集未看剧集，limit 30，
      已完成 show 不出现。
- [ ] `GET /:id`：`watchedMap` 构建（重复 watch 记录去重）、`aired` 标记
      基于当天日期字符串比较、show 不存在 → 404。
- [ ] `GET /:id/seasons`：季列表基本形状。
- [ ] `GET /:showId/episodes/:season/:episode`：TMDB 导演信息（成功/
      `getTmdbEpisodeDetail` 降级两种路径）、Trakt 评分（`show.traktId`
      存在才调用 `getEpisodeRating`，不存在则跳过）、season 内其余集的
      watched 状态集合。
- 验收：新建 `shows-routes.test.ts`（只读部分），mock
  `../services/sync.js`/`../services/trakt.js`/`../services/tmdb.js`；
  覆盖率显著回升（本批次不要求单独跑到 90%，与批次 3 合计验收）。

### 批次 3 — `routes/shows.ts`：写操作路由（watch/reset/mark-watched/force-sync）[ ]

- [ ] `POST .../watch`：插入 watchHistory、调用 `recalcShowProgress`；
      fire-and-forget IIFE 的四个门槛条件各自独立验证（缺 jellyfin 配置/
      autoDeleteLibraryIds 为空/show 状态非 ended-canceled/进度未
      100% completed → 均不调用 `autoDeleteJellyfinEpisode`；四个条件
      同时满足才调用）；IIFE 内部抛错不影响响应已返回的 200。
- [ ] `GET .../episodes/:season/:episode/history` 与
      `GET /:showId/history`：基本返回形状。
- [ ] `DELETE /:showId/history/:historyId`：所有权校验（join 到
      userId）失败 → 404/403；成功删除后调用 `recalcShowProgress`。
- [ ] `POST /:showId/reset`：插入 `watchResetCursors`，调用
      `recalcShowProgress`。
- [ ] `POST /:showId/seasons/:season/mark-watched`：只批量插入本季未看
      集数，`alreadyWatched` 计数只统计目标季内已看的（不应包含其他季）。
- [ ] `POST /:showId/force-sync`：调用 `forceSyncShow(userId, showId)`
      成功/抛错→500 两条路径。
- 验收：延续批次 2 的 `shows-routes.test.ts` 补充写操作部分；
  `routes/shows.ts` 覆盖率 23.11%→90%+ stmts（批次 2+3 合计）。

### 批次 4 — `services/trakt.ts`：薄封装方法（约 40 个）[ ]

- [ ] watchlist：`getWatchlistShows/Movies`、`addToWatchlist`、
      `removeFromWatchlist`。
- [ ] search：`searchShows/Movies`。
- [ ] discovery：`getTrendingShows/Movies`、`getPopularShows/Movies`。
- [ ] ratings：`getRatingsShows/Movies`、`addRating`、`removeRating`。
- [ ] lists：`getLists`、`getListItems`、`createList`、`updateList`、
      `deleteList`、`addListItems`、`removeListItems`。
- [ ] stats/settings：`getTraktStats`、`getUserSettings`。
- [ ] collection：`getCollectionShows/Movies`、
      `removeCollectionShows/Movies`。
- [ ] `getWatchedShows/Movies`。
- [ ] `getHistory`/`getMovieHistory`：分页循环（`X-Pagination-Page-Count`
      头驱动的 `while(true)`），验证多页拼接与单页提前结束两种情况。
- [ ] `getShowProgress`、`getWatching`（只返回 `type === "episode"`
      的条目，过滤掉电影）。
- 验收：新建 `trakt-client.test.ts`，mock `providerFetch` + DB
  chain-mock（`readUserTokens` 短路为一个未过期 token）；每个方法至少
  验证请求 URL/参数拼接正确 + 响应映射正确。

### 批次 5 — `services/trakt.ts`：缓存类方法（4 个）[ ]

- [ ] `getShowDetail`（TTL 7 天，`forceRefresh` 参数，缓存
      source `"trakt_show"`）：新鲜命中跳过 fetch；过期 fetch 并写回；
      `forceRefresh` 无视新鲜度；fetch 失败但在 grace 窗口内→降级返回
      旧缓存；fetch 失败且无缓存/超出 grace→抛出。
- [ ] `getSeasons`（source `"trakt_seasons"`）：同上模式。
- [ ] `getEpisodes`（TTL 24 小时，source `"trakt_episodes"`）：同上模式。
- [ ] `getEpisodeRating`（TTL 24 小时，`RATING_TTL_MS`）：fetch 失败
      降级返回 stale-or-null；0-10 浮点评分转换为 0-100 整数评分的换算
      正确性。
- 验收：并入 `trakt-client.test.ts` 或独立
  `trakt-client-cache.test.ts`；`services/trakt.ts` 覆盖率
  35.46%→90%+ stmts（批次 4+5 合计）。

### 批次 6 — `services/sync.ts`：小型纯函数与进度重算 [ ]

- [ ] `airedWithinForceRefreshWindow`（~61 行）。
- [ ] `getSyncStatus`（~8 行）。
- [ ] `getPostResetWatchedEpisodeIds`（~13 行）、`computeWatchedEpisodes`
      （~5 行）。
- [ ] `recalcShowProgress`（~98 行）、`recalcMovieProgress`（~33 行）。
- [ ] `resetStaleRunningSyncs`（~18 行）。
- 验收：新建 `sync-helpers.test.ts`；这 7 个函数覆盖率达到 90%+。

### 批次 7 — `services/sync.ts`：`syncRatings` + `syncWatchlist` + `syncUserCollection` [ ]

- [ ] `syncRatings`（~95 行）。
- [ ] `syncWatchlist`（~130 行）。
- [ ] `syncUserCollection`（~214 行）。
- 验收：新建 `sync-collections.test.ts`；三个函数各自主流程 + 至少一个
  异常/边界分支被覆盖。

### 批次 8 — `services/sync.ts`：`syncMovies`（~386 行）[ ]

- [ ] 独立批次，因为体量接近 `services/backup.ts` 全文件（该文件此前
      也是拆出单独测试文件处理）。具体分支在批次执行前通读源码后补充。
- 验收：新建 `sync-movies.test.ts`。

### 批次 9 — `services/sync.ts`：`triggerFullSync` + `triggerIncrementalSync` [ ]

- [ ] `triggerFullSync`（~189 行）、`triggerIncrementalSync`（~335 行）。
- [ ] 两者与 `forceSyncShow`（批次 10）的调用关系需要在执行前通过
      codegraph/通读确认，避免批次 9 和批次 10 的测试重复覆盖同一段逻辑
      两次。
- 验收：新建 `sync-triggers.test.ts`。

### 批次 10 — `services/sync.ts`：`forceSyncShow`（~673 行，全仓库最大函数）[ ]

- [ ] 全仓库最大单函数，执行前必须完整通读，很可能需要按内部逻辑段再
      拆成 2-3 个子批次（例如：episode/season 元数据同步 vs. 观看记录
      对账 vs. 收尾状态更新），具体子拆分在通读后于本文件补充细化。
- 验收：新建 `sync-force.test.ts`（或按子批次拆多个文件）；
  `services/sync.ts` 整体覆盖率（批次 6-10 合计）25.27%→80%+ stmts
  （鉴于函数体量巨大，目标定得比其他文件保守一些）。

## 验收标准

- 批次 1-5（history.ts import、shows.ts 全部、trakt.ts 全部）优先执行，
  预期本身就能把 api 整体覆盖率从 69.58% 推高一个明显台阶。
- 批次 6-10（sync.ts）体量最大、风险最高，允许跨会话/跨 plan 文件延续
  执行，但批次边界（14 个函数如何分组）在本计划中已经明确，后续任何
  一次执行都可以直接从某个批次开始，不需要重新做结构分析。
- 每个子批次：api tsc 全绿、全套件测试全绿（只增不减）、独立 commit。
- 不在任何测试里触碰真实 Trakt/TMDB/Jellyfin API 或生产数据库；
  `services/sync.ts` 的测试验证的是同步逻辑的分支正确性，不是真的跑一次
  对生产 Trakt 账号的同步。
- 全部批次完成后（可能跨越多个计划文件），四个文件累计约 4600 行的
  覆盖率缺口清零，api 整体覆盖率预期从 69.58% 大幅回升；届时更新
  `vitest.config.ts` 的 ratchet 阈值与本文件/requirement.md 的收尾小结。
